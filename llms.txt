# Bayesian joint mixed-effects (JoiNMe) model using Stan

`JoiNMe` (with package name styled as `joinme` for deliberate ambiguity)
fits Bayesian nested mixed-effects models for multivariate longitudinal
markers, either alone or jointly with time-to-event outcomes. It
supports multiple outcome families (Gaussian, Student-t, binary, count,
beta, ordinal, skewed families) and irregular measurement schedules.

Current implementation experiments with flexible association structures,
covariance regression, transform-based modelling for nonlinear effects,
and dynamic prediction.

`joinme` works with CmdStanR and RStan. The package is designed with
recipes for:

1.  simulate or assemble longitudinal and event data with
    [`simulate_joinme()`](https://trinhdhk.github.io/joinme/reference/simulate_joinme.md),
2.  fit the joint model with
    [`joinme()`](https://trinhdhk.github.io/joinme/reference/joinme.md),
3.  inspect posterior draws and diagnostics,
4.  summarise and plot fitted effects,
5.  generate dynamic predictions and plot future trajectories or
    survival.

## Marker distributional parameters

By default, `joinme` uses **family-shared** distributional parameters
where possible, i.e.,

- Markers with the same family share one latent parameter for each
  applicable distributional quantity.
- Example: for two Student-`t` markers and one Gaussian marker,
  - Student-`t` markers share one `sigma` and one `nu` process,
  - Gaussian marker has its own `sigma`.

If `formulaDist` is specified for a distributional parameter (for
example `sigma ~ 1 + time`), `joinme` uses that regression structure
instead of a constant family-shared baseline for that parameter. In that
case, summaries report regression terms (fixed/random effects) for the
distributional model. Details below:

## Family-scoped `formulaDist` syntax

You can scope distributional regressions by family using square brackets
on the LHS:

- Global (applies to all rows where parameter exists):
  - `sigma ~ 1 + time`
- Family-scoped (separate regression block):
  - `sigma[family=student_t] ~ 1 + time`
  - `sigma[family=gaussian] ~ 1 + x1`

Allowed distributional parameters:

- `sigma`
- `nu`
- `phi`
- `alpha` (aliases: `alpha_skew`, `skew`)
- `kappa`: the positive Beta sample size, giving shape parameters
  `mu * kappa` and `(1 - mu) * kappa`
- `tau`: the skew-double-exponential quantile/asymmetry parameter in
  `(0, 1)`. It may be modelled with `formulaDist`, or fixed for one
  marker with `jm_family("skew_laplace", tau = 0.8)`.

Example:

``` r

formulaDist <- list(
  sigma[family=student_t] ~ 1 + time + (1 | id),
  sigma[family=gaussian] ~ 1 + x1,
  nu[family=student_t] ~ 1,
  alpha_skew[family=skew_normal] ~ 1 + x1,
  phi[family=negbin2] ~ 1,
  kappa[family=beta] ~ 1,
  tau[family=skew_double_exponential] ~ 1
)
```

For a fixed skew-Laplace quantile, place the constant in the
corresponding marker family rather than in `control`:

``` r

families <- list(
  jm_family("gaussian"),
  jm_family("skew_laplace", tau = 0.8)
)
```

The same `families` declaration can be passed to
[`simulate_joinme()`](https://trinhdhk.github.io/joinme/reference/simulate_joinme.md).
The simulator uses the marker-specific constant to generate outcomes and
records it in the returned distributional truth.

Here `tau = 0.5` gives the symmetric Laplace distribution. Both
endpoints are excluded: `tau = 0` and `tau = 1` are degenerate limits
rather than valid skew-Laplace distributions.

## Installation

``` r

# install.packages("remotes")
# remotes::install_github("trinhdhk/joinme")
```

`joinme` is compatible to run with CmdStanR, but it needs compilation.

``` r

# Preferred: CmdStanR backend
# install.packages("cmdstanr", repos = c("https://stan-dev.r-universe.dev", getOption("repos")))
# cmdstanr::install_cmdstan()
# precompile_cmdstanr_models()
```

### Backend and threading

- At runtime, `joinme` prefers CmdStanR when CmdStan is installed.
- If the preferred backend is unavailable in the current session,
  `joinme` falls back to the other available Stan backend and warns.

### Public fit, prediction, and posterior interfaces

Posterior draws are available through a dedicated renamed-draw
interface:

- `posterior_draws(fit, ...)` and `posterior_draws(pred, ...)` return
  `posterior`-compatible draws
- `as.array(fit)` and `as.array(pred)` return the same renamed posterior
  arrays
- [`mcmc_plot()`](https://trinhdhk.github.io/joinme/reference/mcmc_plot.md)
  forwards those draws to `bayesplot`, imitating the behaviour of `brms`
- [`longitudinal_plot()`](https://trinhdhk.github.io/joinme/reference/longitudinal_plot.md),
  [`survival_plot()`](https://trinhdhk.github.io/joinme/reference/survival_plot.md),
  [`cumhaz_plot()`](https://trinhdhk.github.io/joinme/reference/cumhaz_plot.md),
  [`association_plot()`](https://trinhdhk.github.io/joinme/reference/association_plot.md),
  and
  [`diagnostic_plot()`](https://trinhdhk.github.io/joinme/reference/diagnostic_plot.md)
  provide methods for plotting individual processes.f

## Quick example

``` r


library(joinme)

set.seed(1)

# Simulate a small dataset
sim <- simulate_joinme(
  n_id = 10,
  families = list(
    jm_family("student_t"),
    jm_family("student_t", inv_link = ~ inv_logit(x / 2))
  ),
  times_obs = seq(0, 5, length.out = 8),
  quadrature_nodes = 31,
  seed = 2026,
  assoc = c("cv_total"),
  truth = jm_truth(assoc_coef = list(slope = c(cv_total = 0.6)))
)

# Note: when estimating marker weights, compare to sim$truth$marker_weights
# because effective marker intensities are used directly from w_raw inside Stan.

formulaLong <- y ~ 1 + time + x1 +
  (1 + time | id) +
  (0 + x1 + (1 + time | id) | marker)

formulaEvent <- survival::Surv(time, event) ~ 1 + x1 + x2

fit <- joinme(
  formulaLong = formulaLong,
  dataLong = sim$dataLong,
  formulaEvent = formulaEvent,
  dataEvent = sim$dataEvent,
  assoc = c("cv_total"),
  families = list(
    jm_family("student_t"),
    jm_family("student_t", inv_link = ~ inv_logit(x / 2))
  ),
  transforms = list(cv_total = list(type = "identity")),
  control = list(
    parallel_chains = 1,
    iter_warmup = 100,
    iter_sampling = 100,
    seed = 2026,
    refresh = 0,
    adapt_delta = 0.9
  )
)

inherits(fit, "JoiNMeFit")

# Summaries
summary(fit)
diagnosis(fit)
fixef(fit)
coef(fit)

# Renamed posterior draws
posterior_draws(fit, variables = c("time", "alpha_cv_total"), format = "draws_df")

# Bayesplot-backed posterior display with renamed variables
mcmc_plot(fit, variable = c("time", "alpha_cv_total"), type = "trace")

# Conditional effects for either joint-model process
profiles <- make_conditions(sim$dataEvent, vars = "x1")
ce <- conditional_effects(
  fit,
  effects = list(longitudinal = "time", event = "x1"),
  conditions = profiles,
  process = c("longitudinal", "event"),
  plot = FALSE
)
plot(ce, ask = FALSE)

# Average marker-specific expected responses after applying each inverse link
ce_average_marker <- conditional_effects(
  fit,
  effects = "time",
  process = "longitudinal",
  longitudinal_estimand = "marginal_marker",
  plot = FALSE
)

# Paired posterior contrast between two covariate profiles
contrast_profiles <- data.frame(
  time = seq(0, 5, length.out = 40),
  x2 = 0,
  cond__ = paste0("time=", round(seq(0, 5, length.out = 40), 2))
)
cc <- conditional_contrast(
  fit,
  groupA = c(x1 = 1),
  groupB = c(x1 = -1),
  conditions = contrast_profiles,
  process = c("longitudinal", "event"),
  longitudinal_estimand = "marginal_marker",
  plot = FALSE
)
plot(cc, condition_variable = "time", ask = FALSE)

# Random effects / covariance / combined coefficients (nested by formula block)
re_fit <- ranef(fit)
cf_fit <- coef(fit)
vc_fit <- vcov(fit)
# Example accessors:
# re_fit$formulaLong$id
# cf_fit$formulaLong$id
# re_fit$formulaDist$sigma$allFamilies
# vc_fit$formulaDist$nu$allFamilies

# Dynamic prediction for one subject
ndL <- sim$dataLong[sim$dataLong$id == 1, ]
ndE <- sim$dataEvent[sim$dataEvent$id == 1, ]
time_start <- max(ndL$time)

pred <- posterior_epred(
  fit,
  newdataLong = ndL,
  newdataEvent = ndE,
  time_start = time_start,
  times = seq(time_start, time_start + 1, length.out = 20),
  control = list(n_samples = 50),
  seed = 69
)

# Prediction diagnostics + conditional random-effect summaries
summary(pred)
diagnosis(pred)

# Available only when marker covariance depends on id
if (isTRUE(pred$metadata$marker_corr_depends_on_id)) {
  re_pred <- ranef(pred)
  vc_pred <- vcov(pred)
}

# Plot longitudinal and survival predictions
plot(pred, type = c("longitudinal", "survival"), combined = TRUE)
longitudinal_plot(pred)
survival_plot(pred)

# Alternative longitudinal display: posterior mean change heatmap
plot(
  fit,
  type = "longitudinal",
  longitudinal_style = "heatmap",
  scale = "epred",
  threshold = 0.05
)

# Posterior summary/extraction helpers
posterior_summary(fit)
posterior_assoc(fit, summary = TRUE)

# Conventional coefficient interfaces use posterior_summary() for reporting
# and extract() for their draw-level inputs.
fixef(fit)
ranef(fit)
coef(fit)
extract(fit, what = "fixed_effects")
extract(fit, what = "random_effects")
extract(fit, what = "coefficients")

# Explicit fitted-object plot helpers
association_plot(fit)
diagnostic_plot(fit, type = "rhat")

# All coefficient extractors support summary = TRUE/FALSE
fixef(fit, summary = TRUE)
ranef(fit, summary = FALSE)
coef(fit, summary = FALSE)

# For multiple subjects with combined=TRUE: returns one combined plot per subject
# (named list). If combiner packages are unavailable, falls back to the
# standard subject/outcome nested list.
# New marker levels in newdataLong are rejected; prediction marker levels must
# match training levels. Dynamic prediction summaries marginalize latent
# augmentation noise by averaging across dynpred posterior rows per stored draw.
```

Trinh Dong, 2026

# Package index

## All functions

- [`assoc(`*`<JoiNMeMixFit>`*`)`](https://trinhdhk.github.io/joinme/reference/assoc.JoiNMeMixFit.md)
  : Posterior associations from a latent-progress mixture

- [`assoc()`](https://trinhdhk.github.io/joinme/reference/assoc.md)
  [`posterior_assoc()`](https://trinhdhk.github.io/joinme/reference/assoc.md)
  : Build posterior association effects for a fitted JoiNMe model

- [`association_plot()`](https://trinhdhk.github.io/joinme/reference/association_plot.md)
  : Plot fitted association curves from a JoiNMe fit

- [`bayes_factor()`](https://trinhdhk.github.io/joinme/reference/bayes_factor.md)
  : Bayes factor between two JoiNMe fits

- [`build_standata_transforms()`](https://trinhdhk.github.io/joinme/reference/build_standata_transforms.md)
  : Transform Specification Helper

- [`concordance(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/concordance.JoiNMeFit.md)
  : Concordance of conditional survival predictions

- [`concordance(`*`<JoiNMeMixFit>`*`)`](https://trinhdhk.github.io/joinme/reference/concordance.JoiNMeMixFit.md)
  : Event discrimination for latent-progress mixtures

- [`conditional_contrast()`](https://trinhdhk.github.io/joinme/reference/conditional_contrast.md)
  : Conditional contrasts for fitted JoiNMe models

- [`conditional_effects(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/conditional_effects.JoiNMeFit.md)
  : Conditional effects for a fitted joint model

- [`cumhaz_plot()`](https://trinhdhk.github.io/joinme/reference/cumhaz_plot.md)
  : Plot cumulative hazard trajectories from JoiNMe objects

- [`diagnosis(`*`<JoiNMeMixFit>`*`)`](https://trinhdhk.github.io/joinme/reference/diagnosis.JoiNMeMixFit.md)
  : Diagnostic summary for a latent-progress mixture

- [`diagnosis()`](https://trinhdhk.github.io/joinme/reference/diagnosis.md)
  : Diagnostic summary for JoiNMe objects

- [`diagnostic_plot()`](https://trinhdhk.github.io/joinme/reference/diagnostic_plot.md)
  : Plot posterior diagnostics from a JoiNMe fit

- [`elpd(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/elpd.JoinMeFit.md)
  : ELPD summary for JoiNMe models

- [`example_transform_spec()`](https://trinhdhk.github.io/joinme/reference/example_transform_spec.md)
  : Example: Build Transform Specification from Formula String

- [`extract(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/extract.JoinMeDynPred.md)
  : Extract stored posterior draw components from dynamic prediction
  objects

- [`extract(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/extract.JoinMeFit.md)
  : Extract posterior draws from a fitted JoiNMe model

- [`extract()`](https://trinhdhk.github.io/joinme/reference/extract.md)
  : Extract posterior draws from JoiNMe objects

- [`gk_quadrature()`](https://trinhdhk.github.io/joinme/reference/gk_quadrature.md)
  : Gauss-Kronrod quadrature grid helper

- [`invert_transform_expr()`](https://trinhdhk.github.io/joinme/reference/invert_transform_expr.md)
  : Invert a one-to-one transformation expression

- [`joinme-package`](https://trinhdhk.github.io/joinme/reference/joinme-package.md)
  [`JoiNMe-package`](https://trinhdhk.github.io/joinme/reference/joinme-package.md)
  [`JoiNMe`](https://trinhdhk.github.io/joinme/reference/joinme-package.md)
  : The 'JoiNMe' package.

- [`joinme()`](https://trinhdhk.github.io/joinme/reference/joinme.md) :
  Fit Joint Nested Mixed-effects (JoiNMe) model via cmdstanr or rstan

- [`joinme_basehaz()`](https://trinhdhk.github.io/joinme/reference/joinme_basehaz.md)
  [`jm_basehaz()`](https://trinhdhk.github.io/joinme/reference/joinme_basehaz.md)
  : Build specs for baseline hazard

- [`joinme_family()`](https://trinhdhk.github.io/joinme/reference/joinme_family.md)
  [`jm_family()`](https://trinhdhk.github.io/joinme/reference/joinme_family.md)
  : JoiNMe longitudinal family specification

- [`joinme_mix()`](https://trinhdhk.github.io/joinme/reference/joinme_mix.md)
  : Latent-progress mixtures for nested multivariate joint models

- [`joinme_priors()`](https://trinhdhk.github.io/joinme/reference/joinme_priors.md)
  [`jm_priors()`](https://trinhdhk.github.io/joinme/reference/joinme_priors.md)
  : Declare priors by scientific model component

- [`joinme_standata()`](https://trinhdhk.github.io/joinme/reference/joinme_standata.md)
  : Prepare standata for Joint Nested Mixed-effects (JoiNMe) model.

- [`joinme_tf()`](https://trinhdhk.github.io/joinme/reference/joinme_tf.md)
  [`jm_tf()`](https://trinhdhk.github.io/joinme/reference/joinme_tf.md)
  : Declare association transformations with validation

- [`joinme_truth()`](https://trinhdhk.github.io/joinme/reference/joinme_truth.md)
  [`jm_truth()`](https://trinhdhk.github.io/joinme/reference/joinme_truth.md)
  : Declare population truths for JoiNMe simulation

- [`log_lik(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/log_lik.JoinMeFit.md)
  : Log-likelihood summary

- [`longitudinal_plot()`](https://trinhdhk.github.io/joinme/reference/longitudinal_plot.md)
  : Plot longitudinal trajectories from JoiNMe objects

- [`loo(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/loo.JoinMeFit.md)
  : LOO-CV for JoiNMe models

- [`make_conditions()`](https://trinhdhk.github.io/joinme/reference/make_conditions.md)
  : Build named conditional-effects profiles

- [`marker_weights()`](https://trinhdhk.github.io/joinme/reference/marker_weights.md)
  : Extract individual marker weights

- [`mcmc_plot()`](https://trinhdhk.github.io/joinme/reference/mcmc_plot.md)
  : Plot posterior MCMC summaries using bayesplot

- [`parse_transform_expr()`](https://trinhdhk.github.io/joinme/reference/parse_transform_expr.md)
  : Functional Transform Builder for Joint Models

- [`penalised_ispline_transform()`](https://trinhdhk.github.io/joinme/reference/penalised_ispline_transform.md)
  [`penalized_ispline_transform()`](https://trinhdhk.github.io/joinme/reference/penalised_ispline_transform.md)
  : Penalised Monotone I-spline Transform Spec

- [`plot(`*`<JoiNMeConditionalContrasts>`*`)`](https://trinhdhk.github.io/joinme/reference/plot.JoiNMeConditionalContrasts.md)
  : Plot JoiNMe conditional contrasts

- [`plot(`*`<JoiNMeConditionalEffects>`*`)`](https://trinhdhk.github.io/joinme/reference/plot.JoiNMeConditionalEffects.md)
  : Plot JoiNMe conditional effects

- [`plot(`*`<JoiNMeMixFit>`*`)`](https://trinhdhk.github.io/joinme/reference/plot.JoiNMeMixFit.md)
  : Plot class-specific latent-progress summaries

- [`plot(`*`<JoiNMePosteriorClass>`*`)`](https://trinhdhk.github.io/joinme/reference/plot.JoiNMePosteriorClass.md)
  : Plot posterior class probabilities

- [`plot(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/plot.JoinMeDynPred.md)
  : Enhanced Plot Method for Dynamic Prediction from Joint Models

- [`plot(`*`<PosteriorAssoc>`*`)`](https://trinhdhk.github.io/joinme/reference/plot.PosteriorAssoc.md)
  : Plot posterior association output from a latent-progress fit

- [`posterior_class(`*`<JoiNMeMixDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/posterior_class.JoiNMeMixDynPred.md)
  : Conditional class membership after dynamic prediction

- [`posterior_class()`](https://trinhdhk.github.io/joinme/reference/posterior_class.md)
  : Posterior class membership for a latent-progress fit

- [`posterior_draws()`](https://trinhdhk.github.io/joinme/reference/posterior_draws.md)
  [`as.array(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/posterior_draws.md)
  [`as.array(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/posterior_draws.md)
  : Posterior draws for JoiNMe objects

- [`posterior_interval(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/posterior_interval.JoiNMeDynPred.md)
  : Central posterior credible intervals for dynamic predictions

- [`posterior_interval(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/posterior_interval.JoiNMeFit.md)
  : Central posterior credible intervals for JoiNMe models

- [`posterior_marker_weights()`](https://trinhdhk.github.io/joinme/reference/posterior_marker_weights.md)
  : Posterior individual marker-weight alias

- [`pp_check(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/pp_check.JoinMeFit.md)
  : Posterior predictive check for longitudinal outcomes

- [`precompile_cmdstanr_models()`](https://trinhdhk.github.io/joinme/reference/precompile_cmdstanr_models.md)
  : Precompile CmdStanR models

- [`predict(`*`<JoiNMeMixFit>`*`)`](https://trinhdhk.github.io/joinme/reference/predict.JoiNMeMixFit.md)
  : Predict from a latent-progress mixture

- [`print(`*`<JoiNMeConditionalContrasts>`*`)`](https://trinhdhk.github.io/joinme/reference/print.JoiNMeConditionalContrasts.md)
  : Print a JoiNMe conditional-contrast object

- [`print(`*`<JoiNMeConditionalEffects>`*`)`](https://trinhdhk.github.io/joinme/reference/print.JoiNMeConditionalEffects.md)
  : Print a JoiNMe conditional-effects data object

- [`print(`*`<JoiNMePosteriorClass>`*`)`](https://trinhdhk.github.io/joinme/reference/print.JoiNMePosteriorClass.md)
  : Print posterior class probabilities

- [`print(`*`<JoiNMe_family_spec>`*`)`](https://trinhdhk.github.io/joinme/reference/print.JoiNMe_family_spec.md)
  :

  Method for printing a `JoiNMe_family_spec` object

- [`print(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/print.JoinMeDynPred.md)
  : Print dynamic prediction results

- [`print(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/print.JoinMeFit.md)
  : Print a JoiNMe object

- [`print(`*`<PosteriorAssoc>`*`)`](https://trinhdhk.github.io/joinme/reference/print.PosteriorAssoc.md)
  : Print posterior association effects

- [`print(`*`<joinme_truth>`*`)`](https://trinhdhk.github.io/joinme/reference/print.joinme_truth.md)
  : Print a JoiNMe simulation-truth declaration

- [`print(`*`<summary_JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/print.summary_JoinMeFit.md)
  : Print a summary_JoiNMeFit object

- [`prior_horseshoe()`](https://trinhdhk.github.io/joinme/reference/prior_horseshoe.md)
  : Declare a regularised horseshoe prior

- [`prior_laplace()`](https://trinhdhk.github.io/joinme/reference/prior_laplace.md)
  : Declare a Laplace coefficient prior

- [`prior_lkj()`](https://trinhdhk.github.io/joinme/reference/prior_lkj.md)
  : Declare an LKJ correlation prior

- [`prior_normal()`](https://trinhdhk.github.io/joinme/reference/prior_normal.md)
  : Declare a Normal coefficient prior

- [`prior_student_t()`](https://trinhdhk.github.io/joinme/reference/prior_student_t.md)
  : Declare a Student-t coefficient prior

- [`ranef(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/ranef.JoinMeDynPred.md)
  : Extract predicted random effects from dynamic predictions

- [`run_joinme_example()`](https://trinhdhk.github.io/joinme/reference/run_joinme_example.md)
  : Example runner for JoiNMe

- [`simulate_joinme_mix()`](https://trinhdhk.github.io/joinme/reference/simulate_joinme_mix.md)
  : Simulate data from a latent-progress JoiNMe model

- [`softplus()`](https://trinhdhk.github.io/joinme/reference/softplus.md)
  : Softplus function

- [`stan_rhat.JoiNMeFit()`](https://trinhdhk.github.io/joinme/reference/stan_diagnostics.JoinMeFit.md)
  [`stan_ess.JoiNMeFit()`](https://trinhdhk.github.io/joinme/reference/stan_diagnostics.JoinMeFit.md)
  [`stan_mcse.JoiNMeFit()`](https://trinhdhk.github.io/joinme/reference/stan_diagnostics.JoinMeFit.md)
  : Stan diagnostics for JoiNMe models

- [`summary(`*`<JoiNMeMixFit>`*`)`](https://trinhdhk.github.io/joinme/reference/summary.JoiNMeMixFit.md)
  : Summarise a latent-progress mixture fit

- [`posterior_summary(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/summary.JoinMeDynPred.md)
  [`summary(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/summary.JoinMeDynPred.md)
  : Summary of JoiNMe dynamic prediction

- [`survival_plot()`](https://trinhdhk.github.io/joinme/reference/survival_plot.md)
  : Plot survival trajectories from JoiNMe objects

- [`time_varyring()`](https://trinhdhk.github.io/joinme/reference/time_varyring.md)
  : Declare a time-varying simulation covariate generator

- [`tvROC()`](https://trinhdhk.github.io/joinme/reference/tvROC.md)
  [`tvAUC()`](https://trinhdhk.github.io/joinme/reference/tvROC.md) :
  Time-dependent ROC curves and areas

- [`update(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/update.JoinMeFit.md)
  : Update a JoiNMe fit

- [`vcov(`*`<JoiNMeDynPred>`*`)`](https://trinhdhk.github.io/joinme/reference/vcov.JoinMeDynPred.md)
  : Extract predicted covariance summaries

- [`vcov(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/vcov.JoinMeFit.md)
  : Extract posterior covariance summaries

- [`waic(`*`<JoiNMeFit>`*`)`](https://trinhdhk.github.io/joinme/reference/waic.JoinMeFit.md)
  : WAIC for JoiNMe models

- [`weibull_h0()`](https://trinhdhk.github.io/joinme/reference/weibull_h0.md)
  : Weibull baseline hazard factory

# Articles

### Get started

- [Introduction to Joint Nested Mixed Effects
  Models](https://trinhdhk.github.io/joinme/articles/joinme-intro.md):
- [Joint model
  formulation](https://trinhdhk.github.io/joinme/articles/joinme-model.md):
- [A Example Workflow for Joint Modelling with mutlivariate Longitudinal
  and Survival Data using
  joinme](https://trinhdhk.github.io/joinme/articles/joinme-workflow.md):

### Joint Nested Mixed-effects Model

- [The JoiNMe Framework: Methodology and Statistical
  Theory](https://trinhdhk.github.io/joinme/articles/joinme-theory.md):
- [JoiNMe: Association
  Construction](https://trinhdhk.github.io/joinme/articles/joinme-associations.md):
- [Independent standard-deviation and correlation
  regression](https://trinhdhk.github.io/joinme/articles/joinme-covariance-regression.md):
- [JoiNMe: Usage
  Demo](https://trinhdhk.github.io/joinme/articles/joinme-demo.md):

### Performance and interpretation

- [Conditional effects and contrasts in
  JoiNMe](https://trinhdhk.github.io/joinme/articles/joinme-interpretation.md):
- [Discrimination analysis: concordance, time-dependent ROC curves, and
  AUC](https://trinhdhk.github.io/joinme/articles/joinme-discrimination.md):
