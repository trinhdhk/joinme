# Example runner for JoiNMe

Runs a small workflow: simulate data, build standata, fit the model, and
return both the fit and simulated data. This is intended as a quick
smoke test or reproducible demo with fixed settings.

## Usage

``` r
run_joinme_example()
```

## Value

A list with `fit` and `sim` entries.
