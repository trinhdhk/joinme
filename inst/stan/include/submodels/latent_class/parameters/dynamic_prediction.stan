/**
 * @file include/submodels/latent_class/parameters/dynamic_prediction.stan
 * @brief Document the latent-class parameter stage for prediction.
 *
 * @details
 * Dynamic prediction conditions on posterior draws of all fitted class
 * quantities. It introduces no new latent-class parameter; only new-subject
 * random effects are sampled by the shared prediction programme.
 */
