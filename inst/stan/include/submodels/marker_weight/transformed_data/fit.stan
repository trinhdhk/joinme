/**
 * @file marker_weight/transformed_data/fit.stan
 * @brief Document the marker-weight transformed-data stage.
 *
 * @details
 * Set indices and offsets are fully resolved in R, so no further deterministic marker-weight preparation is needed in Stan.
 */

