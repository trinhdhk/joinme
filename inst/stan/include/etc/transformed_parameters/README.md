# Transformed-parameter helpers

`regression_coefficients.stan` applies the common coefficient-wise prior
transformation and restores names consumed by the scientific submodels.
Mixture class locations remain separate and are appended only by the mixture
master entry point.
