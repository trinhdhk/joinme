% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_map.R
\name{invert_transform_expr}
\alias{invert_transform_expr}
\title{Invert a one-to-one transformation expression}
\usage{
invert_transform_expr(expr)
}
\arguments{
\item{expr}{A one-sided formula, quosure, quoted expression, or character
string containing exactly one occurrence of \code{x}.}
}
\value{
A one-sided formula whose right-hand side is the algebraic inverse
transformation, expressed in \code{x}.
}
\description{
Derives the algebraic inverse of a transformation written as a one-sided
formula in \code{x}.  The result is another one-sided formula and can therefore
be passed directly to \code{\link[=parse_transform_expr]{parse_transform_expr()}}.  JoiNMe uses this function
when \code{link} is supplied as a formula to \code{\link[=joinme_family]{joinme_family()}}.

Inversion proceeds from the outermost operation towards \code{x}.  Composed
transformations are consequently reversed in the correct order.  For
example, \code{~ log(2 * x + 1)} becomes \code{~ (exp(x) - 1) / 2}.

The supported one-to-one elementary pairs are \code{log}/\code{exp},
\code{logit}/\code{inv_logit}, \code{Phi}/\code{inv_Phi} (with \code{pnorm}/\code{qnorm} and
\code{probit} aliases), \code{sinh}/\code{asinh},
\code{tanh}/\code{atanh}, reciprocal, \code{sqrt}/square, and \code{cbrt}/cube.  Addition,
subtraction, multiplication, division, and powers by a finite non-zero
constant are also supported.  Arithmetic branches that do not contain \code{x}
must reduce to numeric constants.  Even integer powers are rejected because
they are not one-to-one on the real line; a deliberately restricted-domain
inverse can instead be declared explicitly through \code{inv_link}.

Expressions containing \code{x} more than once, or globally non-injective
operations such as \code{abs}, \code{sin}, \code{cos}, and \code{cosh}, are rejected.  This is
deliberate: silently selecting one branch would not define a valid
inverse-link over the full response domain.
}
\examples{
invert_transform_expr(~ log(x))
invert_transform_expr(~ log(2 * x + 1))
invert_transform_expr(~ x^3)
}
