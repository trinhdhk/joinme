% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract.JoiNMeDynPred}
\alias{extract.JoiNMeDynPred}
\title{Extract stored posterior draw components from dynamic prediction objects}
\usage{
\method{extract}{JoiNMeDynPred}(
  object,
  what = c("longitudinal", "longitudinal_fitted", "survival", "cumhaz",
    "random_effects_id", "random_effects_marker_id"),
  id = NULL,
  scale = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{JoiNMeDynPred} object.}

\item{what}{Draw block selector: \code{"longitudinal"}, \code{"longitudinal_fitted"},
\code{"survival"}, \code{"cumhaz"}, \code{"random_effects_id"}, \code{"random_effects_marker_id"}.}

\item{id}{Optional character/integer id filter.}

\item{scale}{Optional scale filter for longitudinal blocks (\code{epred}, \code{linpred}, \code{predict}).}
}
\value{
A list with fields:
\itemize{
\item \code{draws}: numeric matrix or list of matrices
\item \code{meta}: extraction metadata
}
}
\description{
Extract stored posterior draw components from dynamic prediction objects.

Unlike \code{draws.JoiNMeDynPred()}, this method does not flatten all stored
prediction draws into one posterior object. Instead, it preserves the semantic
prediction blocks such as longitudinal, survival, cumulative-hazard, and
random-effect payloads, optionally filtered by id or scale.

Use \code{extract.JoiNMeDynPred()} when you want the original prediction-block
structure. Use \code{draws()} when you want a single flattened
\pkg{posterior}-compatible draw object.
}
