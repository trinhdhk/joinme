% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{posterior_interval.JoiNMeFit}
\alias{posterior_interval.JoiNMeFit}
\title{Central posterior credible intervals for JoiNMe models}
\usage{
\method{posterior_interval}{JoiNMeFit}(
  object,
  prob = 0.9,
  what = c("model", "fixef", "ranef", "coef"),
  variables = NULL,
  regex = FALSE,
  draws = NULL,
  seed = 1,
  ...
)
}
\arguments{
\item{object}{A fitted \code{JoiNMeFit} object. Latent-class fits inherit this
method through \code{JoiNMeMixFit}.}

\item{prob}{A single number strictly between zero and one giving the
posterior probability contained in the interval. The default is \code{0.9},
following \code{rstantools}.}

\item{what}{Posterior view to interval: \code{"model"}, \code{"fixef"}, \code{"ranef"},
or \code{"coef"}.}

\item{variables}{Optional character vector selecting friendly parameter
names when \code{what = "model"}.}

\item{regex}{Logical; when \code{TRUE}, interpret \code{variables} as regular
expressions. This argument applies only to \code{what = "model"}.}

\item{draws}{Optional number of posterior draws to retain before computing
the intervals.}

\item{seed}{Integer seed used when posterior draws are subsampled.}

\item{...}{Additional arguments passed to
\code{\link[rstantools:posterior_interval]{rstantools::posterior_interval()}}.}
}
\value{
For \code{what = "model"} or \code{what = "fixef"}, a numeric matrix with one
row per term and two probability-labelled columns. For \code{what = "ranef"}
or \code{what = "coef"}, a nested list of data frames retaining the coefficient
identifiers and containing the same two probability-labelled columns.
}
\description{
Computes central posterior credible intervals through
\code{\link[rstantools:posterior_interval]{rstantools::posterior_interval()}}. The method uses the same scientific
coefficient views as \code{\link[brms:posterior_summary]{posterior_summary()}}: the complete fitted posterior,
population-level coefficients, group-specific deviations, or their combined
coefficients.
}
\details{
For \code{what = "model"}, the returned matrix has one row per friendly posterior
parameter name. The \code{variables} argument selects those names after JoiNMe has
translated the Stan coordinates into their reported statistical terms.

The \code{fixef} view is also rectangular and therefore returns an interval
matrix. The \code{ranef} and \code{coef} views contain several statistically distinct
coefficient tables. Their nested list structure is retained, while each
draw-level \code{value} column is replaced by the two interval limits. This keeps
subject, marker, event, distributional-family, association-term, and
covariance identities explicit.

Every interval is calculated by the default matrix method of
\code{\link[rstantools:posterior_interval]{rstantools::posterior_interval()}}. Consequently, \code{prob} is the total
posterior probability contained between the two central quantiles.
}
