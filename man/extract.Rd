% Generated by hand to match new API
\name{extract}
\alias{extract}
\alias{extract.JoinMeFit}
\alias{extract.JoinMeDynPred}
\title{Extract posterior draws from joinme objects}
\usage{
extract(object, ...)

\method{extract}{JoinMeFit}(
  object,
  what = c("fixef", "gamma_w", "assoc", "distributional", "distributional_regression", "raw"),
  term = NULL,
  variable = NULL,
  draws = NULL,
  seed = 1,
  ...
)

\method{extract}{JoinMeDynPred}(
  object,
  what = c("longitudinal", "longitudinal_fitted", "survival", "cumhaz", "random_effects_id", "random_effects_marker_id"),
  id = NULL,
  scale = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{JoinMeFit} or \code{JoinMeDynPred} object.}
\item{what}{Component selector for extraction.}
\item{term}{Optional summary-style friendly term names (JoinMeFit).}
\item{variable}{Optional raw Stan variable names (JoinMeFit).}
\item{draws}{Optional number of posterior draws to keep (JoinMeFit).}
\item{seed}{Seed used for draw subsetting (JoinMeFit).}
\item{id}{Optional id filter (JoinMeDynPred).}
\item{scale}{Optional scale filter for longitudinal draw blocks (JoinMeDynPred).}
\item{...}{Additional arguments.}
}
\value{
A list with extracted draw payloads.
For \code{JoinMeFit}, returns \code{draws} matrix and \code{term_map}.
For \code{JoinMeDynPred}, returns draw payloads in stored prediction structure.
}
\description{S3 generic and methods for draw-level extraction with friendly names aligned to summary outputs where possible.}
