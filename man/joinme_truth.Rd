% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specs.R
\name{joinme_truth}
\alias{joinme_truth}
\alias{jm_truth}
\title{Declare population truths for JoiNMe simulation}
\usage{
joinme_truth(
  ...,
  intercept = NULL,
  slope = NULL,
  longitudinal = NULL,
  survival = NULL,
  baseline = NULL,
  vcov = NULL,
  marker_weights = NULL,
  assoc_coef = NULL,
  functional = NULL,
  marker = NULL,
  lkj = prior_lkj(1),
  class = NULL,
  basehaz = list(type = "weibull", shape = 1.4, scale = 6),
  re_params = list(id = list(sd = NULL, corr = NULL), marker = list(sd = NULL, corr =
    NULL), dist = list()),
  .validate = TRUE
)

jm_truth(
  ...,
  intercept = NULL,
  slope = NULL,
  longitudinal = NULL,
  survival = NULL,
  baseline = NULL,
  vcov = NULL,
  marker_weights = NULL,
  assoc_coef = NULL,
  functional = NULL,
  marker = NULL,
  lkj = prior_lkj(1),
  class = NULL,
  basehaz = list(type = "weibull", shape = 1.4, scale = 6),
  re_params = list(id = list(sd = NULL, corr = NULL), marker = list(sd = NULL, corr =
    NULL), dist = list()),
  .validate = TRUE
)
}
\arguments{
\item{...}{Named distributional-parameter truths corresponding to the
left-hand sides of \code{formulaDist}, including family or marker selectors.}

\item{intercept, slope}{Global fixed values or \verb{prior_*()} generating
distributions for intercept and non-intercept population coefficients.}

\item{longitudinal}{Fixed values or generating distributions for the
longitudinal population regression. A named list may contain \code{intercept}
and \code{slope} separately.}

\item{survival}{Fixed values or a generating distribution for event-model
covariate slopes.}

\item{baseline}{Fixed values or generating distributions for coefficients
of a formula baseline hazard.}

\item{vcov}{Covariance-regression truth. It may be shared between the
marginal-standard-deviation and correlation regressions, or supplied as
\code{list(sd = ..., corr = ...)}. Each part accepts \code{intercept}, \code{slope}, and
\code{latent} declarations.}

\item{marker_weights}{A named list containing \code{offset}, \code{intercept}, \code{family},
and \code{shared}. \code{offset} is a fixed marker-specific contribution;
\code{intercept} is a fixed or once-drawn common marker-weight location;
\code{family} governs centred unit-scale marker departures; and \code{shared}
determines whether weighted association terms share one set.}

\item{assoc_coef}{Fixed values or a generating distribution for the active
longitudinal--event association coefficients, in their fitted order.}

\item{functional}{Fixed values or generating distributions for affine
transformation intercepts and slopes.}

\item{marker}{A \verb{prior_*()} family declaration for standardised marker-level
effects. Its location and ordinary scale must remain zero and one.}

\item{lkj}{An object created by \code{\link[=prior_lkj]{prior_lkj()}}. Its concentration governs each
random-effect correlation matrix drawn when \code{re_params} omits \code{corr}.}

\item{class}{A named list containing \code{baseline_prob} and \code{slope}, following
the class-membership roles accepted by \code{\link[=jm_prior]{jm_prior()}}. The slope may be fixed
or drawn once for each simulation.}

\item{basehaz}{Baseline-hazard truth. Supply a hazard function, a formula in
\code{time}, a character family name, or a named list such as
\code{list(type = "weibull", shape = 1.4, scale = 6)}.}

\item{re_params}{Named random-effect covariance declarations for \code{id},
\code{marker}, and distributional regressions under \code{dist}. Each ordinary block
accepts \code{sd} and \code{corr}; either may be omitted and drawn once from the
fitted model's corresponding population distribution.}

\item{.validate}{Logical; if \code{TRUE}, check the declaration immediately.}
}
\value{
An object of class \code{joinme_truth} for the \code{truth} argument of
\code{\link[=simulate_joinme]{simulate_joinme()}} or \code{\link[=simulate_joinme_mix]{simulate_joinme_mix()}}.
}
\description{
\code{jm_truth()} describes the population quantities used once to generate a
complete data set. Its coefficient hierarchy follows \code{\link[=jm_prior]{jm_prior()}}: global
\code{intercept} and \code{slope} declarations provide fallbacks, whilst named model
components may replace either role.

A finite numeric vector fixes the corresponding population coefficient. A
\verb{prior_*()} declaration draws one coefficient vector once at the beginning
of a simulation. The realised vector is then held constant for every subject,
marker and observation and is recorded in the returned simulation's \code{truth}.
Thus, a distribution supplied here describes variation between simulated
data sets, not variation between observations within one data set.

The declaration also contains the baseline hazard and the covariance
parameters for ordinary random-effect blocks. If a random-effect standard
deviation is omitted, one value per coefficient is drawn from
\code{Exponential(1)}. If its correlation matrix is omitted, one matrix is drawn
from the LKJ distribution selected by \code{lkj}. These are the same population
distributions used by the Stan fitting model.
}
\examples{
generating_truth <- jm_truth(
  longitudinal = list(
    intercept = 0,
    slope = prior_normal(mu = 0.5, scale = 0.2)
  ),
  survival = c(treatment = -0.4),
  assoc_coef = c(cv_mean = 0.3),
  basehaz = list(type = "weibull", shape = 1.2, scale = 7),
  re_params = list(id = list(sd = NULL, corr = NULL)),
  lkj = prior_lkj(2)
)
}
