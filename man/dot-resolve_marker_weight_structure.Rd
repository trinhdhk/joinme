% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marker-weights.R
\name{.resolve_marker_weight_structure}
\alias{.resolve_marker_weight_structure}
\title{Resolve shared or term-specific marker-weight structures}
\usage{
.resolve_marker_weight_structure(
  marker_weights,
  marker_levels,
  active_terms,
  shared_marker_weights = TRUE,
  estimate_marker_weights = FALSE,
  context = "JoiNMe"
)
}
\arguments{
\item{marker_weights}{User-supplied marker-weight specification.}

\item{marker_levels}{Character marker labels in fitted order.}

\item{active_terms}{Character vector of active weighted association terms.}

\item{shared_marker_weights}{Logical. If \code{TRUE}, all active weighted
association terms share one marker-weight structure. If \code{FALSE}, each active
weighted association term gets its own marker-weight structure.}

\item{estimate_marker_weights}{Logical. If \code{TRUE}, the default base weights
are zero so the latent perturbation represents the full effective weight. If
\code{FALSE}, the default base weights are one.}

\item{context}{Short context string for errors.}
}
\value{
A named list containing per-term vectors, set indices, and the compact
matrix representation used by Stan.
}
\description{
Converts the user-supplied marker-weight specification into a term-indexed
internal representation. This representation is used by both fitting and
simulation so the data-generating process and the fitted hazard use the same
mapping between association terms and marker weights.

Summary:
\enumerate{
\item identify the active weighted association terms,
\item normalise each supplied marker-weight vector into fitted marker order,
\item either map all active terms to one shared weight set or assign one weight
set per active term,
\item return both per-term vectors and the compact set-index representation used
by Stan.
}
}
\keyword{internal}
