% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standata_transforms.R
\name{penalised_ispline_transform}
\alias{penalised_ispline_transform}
\alias{penalized_ispline_transform}
\title{Penalised Monotone I-spline Transform Spec}
\usage{
penalised_ispline_transform(
  x,
  y,
  knots = NULL,
  n_knots = 6,
  degree = 3,
  lambda = 1,
  direction = NULL,
  weights = NULL,
  diff_order = 2
)

penalized_ispline_transform(...)
}
\arguments{
\item{x}{Numeric vector of input values on the raw feature scale to transform.}

\item{y}{Numeric vector of target transformed values at \code{x} (same length as \code{x}).}

\item{knots}{Optional numeric vector of knots (including boundary knots).}

\item{n_knots}{Integer. If knots are not provided, number of knots to use
(including boundary knots). Default 6.}

\item{degree}{Integer spline degree (default 3).}

\item{lambda}{Non-negative smoothness penalty weight (default 1.0).
Larger values produce smoother fitted transforms; smaller values allow
more local curvature.}

\item{direction}{Monotone direction. Accepted values are \code{"increasing"}
and \code{"decreasing"}. When omitted, the direction is inferred from the
supplied \verb{(x, y)} pairs.}

\item{weights}{Optional non-negative weights (same length as x).}

\item{diff_order}{Integer difference order for penalty (default 2).}

\item{...}{Arguments passed to \code{\link[=penalised_ispline_transform]{penalised_ispline_transform()}}.}
}
\value{
A list suitable for \code{build_standata_transforms()}.
}
\description{
Fits a monotone I-spline transformation with a smoothness penalty and
returns a transform specification compatible with \code{build_standata_transforms()}.

This helper is the plug-in constructor for \code{type = "ispline_penalised"}.
Its defaults are \code{n_knots = 6}, \code{degree = 3}, \code{lambda = 1.0},
\code{weights = NULL} (equal weights), and \code{diff_order = 2}.
Returned coefficients follow the same anchored convention as the Stan-
estimated path: increasing splines run from \code{0} to \code{1}, decreasing splines
run from \code{1} to \code{0}, and interior coefficients stay monotone in the chosen
direction.
Example:
\code{penalised_ispline_transform(x = seq(-2, 2, length.out = 50), y = exp(seq(-2, 2, length.out = 50)))}
}
