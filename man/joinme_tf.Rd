% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-wrappers.R
\name{joinme_tf}
\alias{joinme_tf}
\title{Declare association transformations with validation}
\usage{
joinme_tf(..., .validate = TRUE)
}
\arguments{
\item{...}{Named channel specifications.}

\item{.validate}{Logical; if \code{TRUE} (default), validate the resulting
transform declarations immediately.}
}
\value{
An object of class \code{joinme_tf}.
}
\description{
\code{joinme_tf()} is a user-facing wrapper for transformation declarations used by
\code{joinme()} and \code{simulate_joinme()}. It validates channel names, normalises
common shorthands, and returns a structured object that can be passed directly
as the \code{transforms} argument.

Supported channels are \code{cv_total}, \code{cs_total}, \code{cv_mean}, \code{cs_mean},
\code{cv_marker}, \code{cs_marker}, and \code{corr}.

Supported shorthands per channel:
\itemize{
\item \code{"identity"}, \code{"ispline"}, \code{"ispline_penalised"}, \code{"ispline_penalized"},
\code{"pwlin"}, \code{"functional"}
\item a formula such as \code{~ log1p(x)}, which is interpreted as
\code{list(type = "functional", expr = ~ log1p(x))}
\item a full named list specification such as
\code{list(type = "ispline", knots = ..., coeff = ..., degree = 3)}
}
}
\examples{
tf <- joinme_tf(
  cv_total = "identity",
  corr = ~ -x,
  cv_marker = list(type = "pwlin", x = c(-1, 0, 1), y = c(0.2, 1, 0.2))
)
print(tf)
}
