% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{posterior_interval.JoiNMeDynPred}
\alias{posterior_interval.JoiNMeDynPred}
\title{Central posterior credible intervals for dynamic predictions}
\usage{
\method{posterior_interval}{JoiNMeDynPred}(
  object,
  prob = 0.9,
  variables = NULL,
  regex = FALSE,
  draws = NULL,
  seed = 1,
  ...
)
}
\arguments{
\item{object}{A \code{JoiNMeDynPred} dynamic-prediction object. Latent-class
predictions inherit this method through \code{JoiNMeMixDynPred}.}

\item{prob}{A single number strictly between zero and one giving the
posterior probability contained in the interval. The default is \code{0.9},
following \code{rstantools}.}

\item{variables}{Optional character vector selecting friendly parameter
names when \code{what = "model"}.}

\item{regex}{Logical; when \code{TRUE}, interpret \code{variables} as regular
expressions. This argument applies only to \code{what = "model"}.}

\item{draws}{Optional number of posterior draws to retain before computing
the intervals.}

\item{seed}{Integer seed used when posterior draws are subsampled.}

\item{...}{Additional arguments passed to
\code{\link[rstantools:posterior_interval]{rstantools::posterior_interval()}}.}
}
\value{
A numeric matrix with one row per selected prediction quantity and
two probability-labelled interval columns.
}
\description{
Flattens the stored dynamic-prediction draws using \code{\link[=posterior_draws]{posterior_draws()}} and
computes central credible intervals with
\code{\link[rstantools:posterior_interval]{rstantools::posterior_interval()}}. Composite parameter names retain the
subject, marker, prediction scale, and evaluation-time identities.
}
