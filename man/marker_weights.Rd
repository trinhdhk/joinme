% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marker-weights.R
\name{marker_weights}
\alias{marker_weights}
\alias{marker_weights.JoiNMeFit}
\title{Extract individual marker weights}
\usage{
marker_weights(object, ...)

\method{marker_weights}{JoiNMeFit}(object, draws = NULL, seed = 1, digits = 3, summary = TRUE, ...)
}
\arguments{
\item{object}{A fitted JoiNMe model.}

\item{...}{Unused.}

\item{draws}{Optional number of posterior draws.}

\item{seed}{Random seed used when subsetting draws.}

\item{digits}{Number of decimal places used when \code{summary = TRUE}.}

\item{summary}{Logical; return posterior summaries when \code{TRUE}, otherwise a
draws-by-weight matrix.}
}
\value{
A data frame with one row per marker and weight set when
\code{summary = TRUE}; otherwise a posterior draw matrix.
}
\description{
Returns the effective marker weights used by marker-aggregated association
terms. The effective value is the declared marker-specific offset plus the
fitted common weight location and marker-specific departure. When
\code{marker_weights$shared = TRUE}, one shared set is returned once. Otherwise,
each active weighted association term receives its own set.

Individual weights are intentionally kept out of \code{\link[=summary.JoiNMeFit]{summary.JoiNMeFit()}}. The
model summary reports only the common location and within-set spread. This
function and \code{\link[=coef]{coef()}} return effective weights, \code{\link[nlme:fixef]{fixef()}} returns their
fitted common locations, and \code{\link[nlme:ranef]{ranef()}} returns only marker-specific
departures after subtracting the common location and declared offset.
}
