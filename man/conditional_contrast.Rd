% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-contrast.R
\name{conditional_contrast}
\alias{conditional_contrast}
\alias{conditional_contrast.JoiNMeFit}
\title{Conditional contrasts for fitted JoiNMe models}
\usage{
conditional_contrast(x, ...)

\method{conditional_contrast}{JoiNMeFit}(
  x,
  groupA,
  groupB,
  conditions = NULL,
  process = c("longitudinal", "event"),
  prob = 0.95,
  robust = TRUE,
  method = c("posterior_epred", "posterior_linpred"),
  longitudinal_estimand = c("population", "marker", "marginal_marker"),
  event_scale = c("hazard_ratio", "log_hazard_ratio"),
  markers = NULL,
  draws = NULL,
  summary = TRUE,
  reuse_fitted_re = FALSE,
  seed = 1,
  plot = TRUE,
  ...
)
}
\arguments{
\item{x}{A fitted object inheriting from \code{JoiNMeFit}.}

\item{...}{Additional arguments passed to
\code{\link[=plot.JoiNMeConditionalContrasts]{plot.JoiNMeConditionalContrasts()}} when \code{plot = TRUE}.}

\item{groupA}{Named atomic vector or named list containing one scalar value
for every covariate fixed to define group A.}

\item{groupB}{Named atomic vector or named list containing one scalar value
for every covariate fixed to define group B.}

\item{conditions}{Optional data frame containing the common predictor values
at which the contrast is evaluated. There is one posterior contrast for
every row. A \code{cond__} column supplies display labels; otherwise informative
row names or numbered labels are used. Values not supplied by either a
group or a condition row are derived from the fitting data in the same way
as \code{\link[=conditional_effects.JoiNMeFit]{conditional_effects.JoiNMeFit()}}.}

\item{process}{Character vector selecting \code{"longitudinal"}, \code{"event"}, or
both. \code{"survival"} is accepted as an alias for \code{"event"}, and \code{"both"}
selects both fitted processes.}

\item{prob}{Probability covered by the equal-tailed posterior uncertainty
interval. The default is \code{0.95}.}

\item{robust}{Logical. If \code{TRUE}, posterior medians define \code{estimate__}; if
\code{FALSE}, posterior means are used.}

\item{method}{Longitudinal posterior scale. \code{"posterior_epred"} compares
expected responses after applying the marker-specific inverse link;
\code{"posterior_linpred"} compares longitudinal linear predictors.}

\item{longitudinal_estimand}{Longitudinal quantity entering the comparison.
\code{"population"} uses population coefficients only and averages the selected
marker trajectories within each draw. \code{"marker"} also includes the fitted
marker-level deviation and retains a contrast for each selected marker.
\code{"marginal_marker"} includes that deviation and averages the selected
marker-specific predictions within each draw. Subject and marker-by-subject
deviations are excluded unless \code{reuse_fitted_re = TRUE} selects a fitted
subject.}

\item{event_scale}{Event-process contrast. \code{"hazard_ratio"} reports
\code{exp(eta_A - eta_B)} and \code{"log_hazard_ratio"} reports \code{eta_A - eta_B}, where
\code{eta} is the direct event-regression linear predictor.}

\item{markers}{Optional character vector restricting longitudinal marker
levels. With \code{longitudinal_estimand = "population"} or
\code{"marginal_marker"}, these are the marker trajectories averaged within
each posterior draw.}

\item{draws}{Optional positive integer limiting the posterior draws used in
the paired calculation. \code{NULL} uses all available draws.}

\item{summary}{Logical. If \code{TRUE}, return posterior centres, standard errors,
and credible intervals. If \code{FALSE}, each process contains a tidy data frame
with one row per posterior draw and estimand. The \code{.draw} and \code{.value}
columns identify the retained draw and its value. Draw-level results are
returned without plotting.}

\item{reuse_fitted_re}{Logical. If \code{TRUE}, \code{conditions} must contain a fitted
subject identifier. The paired comparison then includes that subject's
posterior subject and marker-by-subject effects without fitting new random
effects. The identifier cannot be changed between \code{groupA} and \code{groupB}.}

\item{seed}{Integer seed used when posterior draws are subsampled.}

\item{plot}{Logical. If \code{TRUE} and \code{summary = TRUE}, construct and display
contrast plots. If \code{FALSE}, return the \code{JoiNMeConditionalContrasts}
object. \code{summary = FALSE} always returns draw-level results without
plotting.}
}
\value{
With \code{summary = TRUE} and \code{plot = FALSE}, a
\code{JoiNMeConditionalContrasts} object containing
one data frame per requested process. Every table includes \code{estimate__},
\code{se__}, \code{lower__}, and \code{upper__}, together with condition, group, marker,
event-type, and estimand labels where applicable. With \code{summary = FALSE},
every process is instead a tidy draw-level data frame containing \code{.draw},
\code{.value}, \code{estimand__}, and the applicable scientific descriptors. With
\code{summary = TRUE} and \code{plot = TRUE}, the plotting method invisibly returns
either the named list of \code{ggplot} objects or an arranged \code{patchwork}
display.
}
\description{
\code{conditional_contrast()} compares two named covariate profiles within every
posterior draw and only then summarises the resulting contrast. This ordering
retains posterior dependence between the two counterfactual predictions and
gives the appropriate uncertainty for their paired difference or hazard
ratio.

\code{groupA} and \code{groupB} describe the covariate values that distinguish the two
profiles. Other model predictors are taken from each row of \code{conditions},
when supplied, or from reference values of the fitting data. Group values
take precedence when a variable also appears in \code{conditions}. A named list is
useful when values have different types; a named atomic vector is accepted
for concise specifications such as
\code{c(treatment = "active", sex = "female")}.

For the longitudinal process, the reported contrast is group A minus group B
on the scale selected by \code{method}. For the event process,
\code{event_scale = "hazard_ratio"} reports the direct covariate hazard ratio of
group A relative to group B, whereas \code{"log_hazard_ratio"} reports its
logarithm. As in \code{\link[=conditional_effects.JoiNMeFit]{conditional_effects.JoiNMeFit()}}, the event calculation
concerns the covariate component of \code{formulaEvent}; the baseline hazard and
longitudinal association contribution are held common between profiles.
}
\details{
Factor levels, transformations, spline bases, time scaling, and coefficient
ordering are recovered from the fitted model. The two profile designs are
evaluated in one posterior calculation so their columns refer to precisely
the same MCMC draws. The contrast is then formed draw by draw. Credible
intervals therefore describe the posterior distribution of the contrast,
rather than a subtraction of separately summarised intervals.

If \code{conditions} is omitted, numeric predictors use their observed mean and
categorical predictors use their first fitted level. Multiple condition rows
may describe distinct profiles or a trajectory over a continuous variable
such as follow-up time. The plotting method can select such a variable with
\code{condition_variable}.
}
\examples{
\dontrun{
treatment_contrast <- conditional_contrast(
  fit,
  groupA = c(treatment = "active"),
  groupB = c(treatment = "control"),
  process = c("longitudinal", "event"),
  plot = FALSE
)
print(treatment_contrast)
plot(treatment_contrast, arrange = "row", guides = "collect")

time_profiles <- data.frame(
  time = seq(0, 24, length.out = 50),
  cond__ = paste0("month ", seq(0, 24, length.out = 50))
)
treatment_over_time <- conditional_contrast(
  fit,
  groupA = list(treatment = "active", sex = "female"),
  groupB = list(treatment = "control", sex = "female"),
  conditions = time_profiles,
  process = "longitudinal",
  method = "posterior_epred",
  longitudinal_estimand = "marginal_marker",
  plot = FALSE
)
plot(treatment_over_time, condition_variable = "time")
}

}
\seealso{
\code{\link[=conditional_effects.JoiNMeFit]{conditional_effects.JoiNMeFit()}}, \code{\link[brms:make_conditions]{make_conditions()}}
}
