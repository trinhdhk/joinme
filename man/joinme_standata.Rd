% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standata.R
\name{joinme_standata}
\alias{joinme_standata}
\title{Prepare standata for Joint Nested Mixed-effects (JoiNMe) model.}
\usage{
joinme_standata(
  formulaLong,
  dataLong,
  formulaEvent,
  dataEvent,
  formulaVCov = ~1,
  formulaDist = NULL,
  id_var = "id",
  marker_var = "marker",
  time_var = "time",
  eps_fd = 0.01,
  assoc = c("cv_mean"),
  families = NULL,
  transforms = NULL,
  prior_specification = NULL,
  allow_marker_crosscorr = 1L,
  basehaz = c("bs", "ns", "formula"),
  basehaz_n_knots = 5L,
  basehaz_knots = NULL,
  basehaz_degree = 3L,
  basehaz_formula = ~1 + time,
  tau_spline = 0.4,
  quadrature_nodes = NULL,
  vcov_diag_link = c("softplus", "exp"),
  mixture = NULL,
  include_survival = TRUE,
  seed = .Random.seed[[1]]
)
}
\arguments{
\item{formulaLong}{Longitudinal formula defining fixed effects, id-level effects,
and the marker block. The marker block may optionally include an inner
\code{( ... | id )} term for marker-by-id random effects. When the inner term is
omitted, marker-by-id random effects are disabled (Q_idm = 0). Use \code{||} to
enforce independence. In nested marker terms, outer \code{( ... || marker )}
keeps marker-only and marker-by-id blocks independent, while inner
\code{( ... || id )} makes the marker-by-id covariance diagonal. Grouping
terms may use \verb{weighted(group, weights = <column>)}
to define formula-scoped positive subject/group weights.}

\item{dataLong}{Long-format longitudinal data with columns for id, marker, time,
outcome, and covariates referenced in \code{formulaLong}.}

\item{formulaEvent}{Survival formula for baseline covariates and event model.
Supported LHS forms are:
\itemize{
\item \code{survival::Surv(time, status)}
\item \code{survival::Surv(start, stop, status)}
\item \code{survival::Surv(time, status, type = "left")}
\item \code{survival::Surv(time1, time2, type = "interval2")}
The former \code{type = "interval"} form is intentionally rejected.
For an interval-censored event in \eqn{(L,R]}, the prepared data contain
an event-free row from zero to \eqn{L} and a failure-within-interval row
from \eqn{L} to \eqn{R}. Their log-likelihood contributions sum to
\eqn{\log\{S(L)-S(R)\}}. This is a full event likelihood, not a Cox
partial likelihood.
}}

\item{dataEvent}{Event-process data with either one row per id
(\code{Surv(time, status)}) or multiple time-split rows per id
(\code{Surv(start, stop, status)}).
Covariates referenced in \code{formulaEvent} may vary by interval in the split
form. Left- and interval-censored responses require one row per id and
presently describe one event type.}

\item{formulaVCov}{Covariance regression specification for id-specific
marker-by-id effects. A formula applies the same observed covariates to
both components. A named list, \code{list(sd = ~ ..., corr = ~ ...)}, regresses
standard deviations and off-diagonal partial correlations independently.
If the marker block omits the inner \code{( ... | id )}, then marker-by-id effects
are absent and covariance-style associations (\code{corr}, \code{vcov}) are not allowed.
Downstream, \code{corr} uses the off-diagonal entries of the subject-specific
Cholesky-correlation factor \code{K}, whereas \code{vcov} uses those off-diagonal \code{K}
entries together with the subject-specific standard deviations. If both
\code{corr} and \code{vcov} are requested, \code{vcov} is kept and \code{corr} is ignored with a
warning. The default \code{~ 1} is valid and yields an intercept-only covariance
regression with no subject-level slope columns.}

\item{formulaDist}{Optional list of formulas for distributional regression.
Supported parameters are \code{sigma}, \code{nu}, \code{phi},
\code{alpha}, \code{kappa}, and \code{tau}. Here \code{nu} is reserved
for Student-t degrees of freedom; \code{kappa} is the positive Beta
sample-size parameter defining shapes \eqn{\mu\kappa} and
\eqn{(1-\mu)\kappa}; and \code{tau} is the skew-double-exponential
quantile/asymmetry parameter in \eqn{(0,1)}.

Three forms are supported:
\enumerate{
\item Named list with RHS-only formulas, e.g. \code{list(sigma = ~ 1 + time)}.
\item Unnamed list with LHS parameter names, e.g. \code{list(sigma ~ 1 + time)}.
Random-effects terms with \code{|} are supported; nested random-effects formulas are not.
Grouping factors in distributional random-effects terms may also use
\verb{weighted(group, weights = <column>)}.
}}

\item{id_var}{Column name for subject id in both longitudinal and event data.}

\item{marker_var}{Column name for marker/biomarker id in longitudinal data.}

\item{time_var}{Column name for longitudinal time in \code{dataLong}.}

\item{eps_fd}{Positive finite-difference step for association derivatives.}

\item{assoc}{Character vector specifying association components
(e.g., "cv_mean", "cs_total", "corr", "vcov"). The \code{corr} channel
targets off-diagonal entries of the subject-specific Cholesky-correlation
factor \code{K}; the \code{vcov} channel targets those off-diagonal \code{K} entries
together with the subject-specific standard deviations.}

\item{families}{Optional marker-specific family specification. Can be
character family names or \code{jm_family(...)} entries with per-marker links.
If NULL, all markers use Gaussian responses with identity link.
Supported named forward links are \code{identity}, \code{log}, \code{logit}, \code{probit},
and \code{exp}; \code{jm_family()} also accepts an invertible formula link or a
directly specified inverse-link formula. In formula syntax,
\code{inv_Phi}/\code{qnorm}/\code{probit} denote the standard normal quantile and
\code{Phi}/\code{pnorm} denote the standard normal CDF. Thus a probit forward link
is inverted to \code{Phi} before Stan data are constructed. A skew-Laplace
marker may additionally fix its quantile/asymmetry parameter with
\code{jm_family("skew_laplace", tau = 0.8)}. Fixed values must lie strictly
between zero and one and are carried separately for each marker.}

\item{transforms}{Optional list specifying transformations for association terms.
Each element (cv_total, cs_total, corr, vcov) is a list with a \code{type} and fields
required by that type (see \code{build_standata_transforms()}). For covariance-style
terms, transforms apply either to off-diagonal \code{K} features (\code{corr}) or to the
combined off-diagonal \code{K} plus subject-specific SD features (\code{vcov}).
Functional transforms may request fit-only affine-shift parameters through
\code{intercept = TRUE} and/or \code{slope = TRUE} inside the transform formula.
Ordered piecewise-linear fits use
\code{list(type = "pwlin", knots = ..., direction = "increasing")} (or
\code{"decreasing"}). Their knot ordinates are estimated from simplex
increments; the earlier \code{x} field is accepted as an alias for \code{knots}, while
the earlier \code{y} field no longer fixes the fitted curve.}

\item{prior_specification}{A component-based \code{\link[=jm_priors]{jm_priors()}} declaration.
Global \code{intercept} and \code{slope} declarations are inherited unless a
scientific component supplies the corresponding role explicitly.
Marker-weighted associations are governed entirely by its
\code{marker_weights} component. \code{marker_weights$offset} is a wholly named or
wholly unnamed numeric vector; with term-specific weights it may instead
be a named collection indexed by \code{cv_total}, \code{cs_total}, \code{cv_marker}, or
\code{cs_marker}. \code{marker_weights$family = "constant"} (or \code{"none"}) uses the
offset exactly and fits no marker-weight coefficient. A stochastic family
fits \verb{offset + common location + unit-scale marker departure}, with the
common-location fitting prior in \code{marker_weights$intercept}. It does not
supply a population value to simulation. The association slope
supplies the multiplier for the completed weighted marker feature.}

\item{allow_marker_crosscorr}{Integer flag; 1 allows cross-marker correlation in marker RE.}

\item{basehaz}{Baseline hazard basis type: "bs", "ns", or "formula".}

\item{basehaz_n_knots}{Number of internal knots for spline baseline hazards.}

\item{basehaz_knots}{Optional numeric vector of internal knots for spline baseline hazards.}

\item{basehaz_degree}{Degree of spline basis for baseline hazard.}

\item{basehaz_formula}{Formula for baseline hazard when \code{basehaz = "formula"}.}

\item{tau_spline}{Prior scale for spline coefficients (penalised spline).}

\item{quadrature_nodes}{Optional positive integer target for total quadrature
points. Allowed values are exactly \code{7}, \code{15}, \code{31}, \code{41}, \code{51}, and \code{61}.}

\item{vcov_diag_link}{Link for the subject-specific standard deviation regression:
"softplus" or "exp".}

\item{mixture}{Internal latent-progress mixture specification. The public
interface is \code{\link[=joinme_mix]{joinme_mix()}}; ordinary \code{\link[=joinme]{joinme()}} calls leave this as
\code{NULL}, which contributes no mixture parameters or mixture prior.}

\item{include_survival}{Logical indicator that the supplied event rows
represent an observed survival process. The fitting entry points set this
to \code{FALSE} only when they have constructed an internal likelihood-neutral
scaffold for a longitudinal-only analysis.}

\item{seed}{Optional random seed for deterministic components of standata.}

\item{flag_resid_dim}{Integer flag to include residual dimension checks.}
}
\description{
This function process the input from \code{fit} to prepare standata for Joint Nested Mixed-effects (JoiNMe) model.
}
\details{
This builder performs three key steps:
\enumerate{
\item Constructs fixed-effect and random-effect design matrices.
\item Creates distributional regression matrices (including optional random effects).
\item Assembles spline bases and Gauss-Kronrod nodes for survival integration.
}
}
