% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.JoiNMeFit}
\alias{predict.JoiNMeFit}
\alias{posterior_linpred.JoiNMeFit}
\alias{posterior_epred.JoiNMeFit}
\alias{posterior_predict.JoiNMeFit}
\title{Dynamic Prediction for Joint Models}
\usage{
\method{predict}{JoiNMeFit}(
  object,
  newdataLong,
  newdataEvent,
  process = c("longitudinal", "event"),
  pred_type = c("per_marker_id", "marginal_marker", "marginal_id", "marker_subject",
    "subject_marker"),
  scale = c("epred", "linpred", "predict"),
  times = NULL,
  time_start = NULL,
  time_horizon = NULL,
  tmax = NULL,
  ci_levels = c(0.5, 0.95),
  control = list(),
  seed = .Random.seed[[1]],
  ...
)

\method{posterior_linpred}{JoiNMeFit}(object, ...)

\method{posterior_epred}{JoiNMeFit}(object, ...)

\method{posterior_predict}{JoiNMeFit}(object, ...)
}
\arguments{
\item{object}{A fitted object of class \code{JoiNMeFit}.}

\item{newdataLong}{Data frame containing longitudinal histories for one or more subjects.
Must contain columns for id, time, marker, and response variables as specified in the original model formula.}

\item{newdataEvent}{Data frame containing event information and covariates used
in the survival model. It may contain one row per subject or interval-split
rows (\code{Surv(start, stop, status)} layout). Left- and interval-censored
survival encodings (\code{type = "left"}, \code{type = "interval2"}) are accepted.
When interval rows are supplied, dynamic prediction uses the latest row per
subject for event-side covariates.}

\item{process}{Character vector specifying which predictions to compute.
Options: "longitudinal" (future trajectory), "event" (conditional survival probability). Default: both.}

\item{pred_type}{Character. Type of longitudinal predictions:
\itemize{
\item "per_marker_id" (default): Subject-specific predictions for each marker.
\item "marginal_marker": Average across subjects for each marker (population-level by marker).
\item "marginal_id": Average across markers for each subject (subject-specific average).
\item "marker_subject": Alias for "marginal_marker".
\item "subject_marker": Alias for "marginal_id".
}}

\item{scale}{Character vector. Longitudinal prediction scale(s):
\itemize{
\item "epred": expected response (inverse-link),
\item "linpred": linear predictor,
\item "predict": predictive draw (includes noise).
Defaults to all scales when not explicitly set.
For \code{epred}/\code{predict}, family-specific inverse-links configured in
\code{families} (via \code{jm_family(...)}) are respected.
}}

\item{times}{Numeric vector of times at which to predict the longitudinal and survival trajectories.
Can also be a named list of numeric vectors (one per subject id). If NULL, a grid
from \code{time_start} to \code{time_start + time_horizon} is generated (and truncated to
training support when needed). If fewer than 50 points are supplied for a subject,
a warning is emitted when \code{control$n_times = 50}.}

\item{time_start}{Numeric scalar or character. The conditioning time (last observation time).
If numeric, a single value is reused for all subjects, or a named vector supplies
subject-specific values. If character, it is interpreted as a column name in
\code{newdataEvent} holding subject-specific conditioning times. If NULL, defaults to
the maximum observed time in \code{newdataLong} for each subject; for delayed-entry
counting-process data with strictly positive entry times, it defaults to the
subject entry time.}

\item{time_horizon}{Numeric scalar. Prediction horizon (in time units) used when
\code{times} is NULL. The default is \code{tmax}.}

\item{tmax}{Numeric scalar. The maximum time used for scaling during model fitting.
If this is not provided and cannot be inferred from the object, predictions will be on the wrong time scale.}

\item{ci_levels}{Numeric vector of credible interval levels for plotting.
Must be strictly between 0 and 1.}

\item{control}{Named list for prediction configuration.
\itemize{
\item cmdstanr::model$sample() arguments (e.g., \code{chains}, \code{parallel_chains},
\code{iter_warmup}, \code{iter_sampling}, \code{seed}, \code{refresh}, \code{adapt_delta}).
Values override defaults except \code{data}.
\item engine: "cmdstanr" or "rstan". Defaults to options(stan_preferred_engine).
\item n_samples: integer; number of posterior parameter draws extracted from the
fitted object before dynamic prediction. Default 200.
\item n_times: integer; number of points in the prediction time grid when
\code{times} is \code{NULL}. Default 50.
\item n_pred_draws: integer; number of prediction draws produced by the dynpred
object. Default to n_samples. Changing this can destabilise the results.
This is independent of \code{n_samples} (posterior parameter draw
extraction count).
\item threads_per_chain: integer; the threaded dynpred Stan program is always
used. \code{threads_per_chain = 1} keeps execution serial while preserving the
thread-capable kernel. For engine = "rstan", threading uses
options(stan.thread = threads_per_chain).
\item grainsize: integer; reduce_sum grainsize for threaded prediction.
Defaults to the full prediction draw count when \code{threads_per_chain = 1},
and to \code{max(1, ceiling(n_pred_draws/(4*threads_per_chain*chains)))}
otherwise.
\item quadrature_nodes: optional positive integer target for total quadrature
points during dynamic prediction. Allowed values are exactly \code{7}, \code{15},
\code{31}, \code{41}, \code{51}, and \code{61}. Only the node count is passed to Stan; GK
nodes/weights are fixed in the Stan code.
\item progress: logical; show sampling progress bar (default TRUE).
\item initialisation fallback (cmdstanr): if a subject-level dynamic prediction
chain fails to initialise, prediction automatically retries that subject
with a narrower random init range (\code{init = 0.1}).
}}

\item{seed}{Integer. Random seed for reproducibility of random effect sampling.}

\item{...}{Additional arguments passed to \link{predict}.}
}
\value{
A list with two components:
\item{longitudinal}{A data.frame containing longitudinal predictions (Mean, Median, SD, 95\% CrI) for each time point in \code{times}. Columns: id, time, marker, Estimate, Median, Est.Error, L95, U95.}
\item{longitudinal_fitted}{A data.frame containing fitted values for observed history on the linpred and epred scales. Includes a \code{scale} column.}
\item{survival}{A data.frame containing survival probabilities (S(t|time_start)) for each time point in \code{times}. Columns: id, time, Survival, Median, Est.Error, L95, U95.}
\item{cumhaz}{A data.frame containing conditional cumulative hazards H(t|time_start). Columns: id, time, Cumhaz, Median, Est.Error, L95, U95.}
\item{draws}{A list containing raw posterior draws (\code{longitudinal}, \code{longitudinal_fitted}, \code{survival}, \code{cumhaz}) and reconstructed subject-level random effects (\code{random_effects_id}, \code{random_effects_marker_id}, including marker-by-id covariance draws when id-dependent covariance is active).}

A \code{JoiNMeDynPred} object with \code{metadata$scale = "linpred"} and
\code{metadata$scales = "linpred"}.

A \code{JoiNMeDynPred} object with \code{metadata$scale = "epred"} and
\code{metadata$scales = "epred"}.

A \code{JoiNMeDynPred} object with \code{metadata$scale = "predict"} and
\code{metadata$scales = "predict"}.
}
\description{
Performs dynamic prediction for a new subject using a fitted \code{JoiNMeFit} joint model.
It calculates the posterior predictive distribution of longitudinal trajectories
and survival probabilities conditional on the subject's observed history up to a specific time point.

Convenience wrapper around the \link{predict} method returning the posterior
linear predictor (linpred scale).

Convenience wrapper around the \link{predict} method returning the posterior
expected predictor (epred scale).

Convenience wrapper around the \link{predict} method returning posterior
predictive draws (includes observation noise).
}
\details{
\code{predict.JoiNMeFit()} does not accept a \code{condition} argument. It estimates
subject-specific future trajectories and survival conditional on the
longitudinal histories supplied in \code{newdataLong}. Use
\code{\link[=conditional_effects.JoiNMeFit]{conditional_effects.JoiNMeFit()}} with \code{\link[=make_conditions]{make_conditions()}} when the target is
a population-level comparison across named covariate profiles.

The function uses a Bayesian approach in two draw layers:
\enumerate{
\item Extract posterior parameter draws from the fitted object (\code{n_samples}).
\item Re-index those draws to the dynpred draw count (\code{control$n_pred_draws}),
allowing independent control of prediction Monte Carlo size.
\item For each dynpred draw, sample subject-specific random effects
\emph{conditional} on observed history in \code{newdataLong}.
\item Evaluate longitudinal and survival quantities on requested future grids.
\item Pool draws to form marginal predictive summaries and intervals.
}

Dynamic prediction passes the fitted, draw-specific transform ordinates to
Stan. Ordered piecewise-linear associations therefore use the same posterior
curve as the fitted event model and never reconstruct a curve from legacy
user-supplied \code{y} values.

The baseline-hazard basis is likewise inherited from fitting.  Prediction
reuses the retained B-spline or natural-spline object, or the retained
baseline formula, together with the original column-centring constants.
This guarantees that the quadrature arrays have the same number and meaning
of baseline-hazard columns as the fitted Stan parameters.
}
\keyword{internal}
