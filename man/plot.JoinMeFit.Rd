% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.JoiNMeFit}
\alias{plot.JoiNMeFit}
\title{Plot diagnostics, fitted trajectories, and association curves for JoiNMe fits}
\usage{
\method{plot}{JoiNMeFit}(
  x,
  type = c("rhat", "ess_bulk", "ess_tail", "mcse_mean", "mcse_sd", "running_mean",
    "running_quantile", "mcmc", "longitudinal", "survival", "cumhaz", "association"),
  pars = NULL,
  regex_pars = NULL,
  draws = 400,
  seed = 1,
  max_vars = 4,
  mcmc_type = c("intervals", "areas", "dens", "dens_overlay", "hist", "trace", "violin",
    "acf", "rhat", "neff"),
  quantile_probs = c(0.1, 0.5, 0.9),
  subject = NULL,
  marker = NA,
  scale = NULL,
  longitudinal_style = c("curves", "heatmap"),
  longitudinal_times = NULL,
  longitudinal_points = 80L,
  threshold = 0.05,
  smooth_trajectory = TRUE,
  smooth_method = c("loess", "spline"),
  smooth_span = 0.3,
  ci_levels = c(0.5, 0.95),
  ci_type = c("ribbon", "line", "both"),
  observed_first = TRUE,
  facet_by = c("marker", "none"),
  facet_scales = "free_y",
  combined = TRUE,
  show_data = TRUE,
  show_observed_line = TRUE,
  observed_style = list(color = "black", shape = 21, size = 2, alpha = 0.6),
  prediction_style = list(color = "steelblue", fill = "steelblue", linewidth = 0.8, alpha
    = 0.2),
  theme_fn = ggplot2::theme_bw,
  palette_marker = NULL,
  association_options = list(),
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{JoiNMeFit}.}

\item{type}{Plot type. Diagnostic types are \code{"rhat"}, \code{"ess_bulk"},
\code{"ess_tail"}, \code{"mcse_mean"}, \code{"mcse_sd"}, \code{"running_mean"}, and
\code{"running_quantile"}. Fitted-data types are \code{"longitudinal"},
\code{"survival"}, \code{"cumhaz"}, and \code{"association"}. \code{"mcmc"} delegates to
\code{\link[brms:mcmc_plot]{mcmc_plot()}} for bayesplot-backed posterior displays. The compatibility alias
\code{"longitudinal_heatmap"} is treated as
\verb{type = "longitudinal", longitudinal_style = "heatmap"}.}

\item{pars}{Optional character vector of parameter names to include for
diagnostic plots.}

\item{regex_pars}{Optional regular expression for parameter selection for
diagnostic plots.}

\item{draws}{Optional number of posterior draws to subset for diagnostics.
For fitted-data plotting types (\code{"longitudinal"}, \code{"survival"},
\code{"cumhaz"}), this also controls the number of fitted posterior draws
used to build plot summaries directly from the fitted object.}

\item{seed}{Random seed for draw subsetting and fitted plotting.}

\item{max_vars}{Maximum number of parameters for running diagnostics.}

\item{mcmc_type}{Bayesplot geometry used when \code{type = "mcmc"}.}

\item{quantile_probs}{Numeric vector of quantile probabilities for running
quantile plots.}

\item{subject}{Optional vector of subject ids for fitted-data plots.}

\item{marker}{Optional marker subset for longitudinal and association plots.
Use a specific marker level to plot only that marker, or \code{NA} to plot all
markers.}

\item{scale}{Longitudinal scale for fitted-data plots. One of \code{"epred"},
\code{"linpred"}, or \code{"predict"}.}

\item{longitudinal_style}{Display style for fitted longitudinal plots.
\code{"curves"} shows the existing separated trajectory curves with credible
bands. \code{"heatmap"} shows marker-level mean change over time using draw-level
aggregation across the selected subjects. The heatmap is therefore an
alternative display for the longitudinal process rather than a separate
modelling target.}

\item{longitudinal_times}{Optional numeric vector of times at which fitted
longitudinal trajectories are evaluated. Times are expressed on the
original study-time scale. When \code{NULL}, an evenly spaced sequence spanning
the selected subjects' observed follow-up is used.}

\item{longitudinal_points}{Number of evenly spaced evaluation times used when
\code{longitudinal_times = NULL}. The default is 80.}

\item{threshold}{Posterior sign-certainty threshold for
\code{longitudinal_style = "heatmap"}. A tile is shown as significant when the
posterior probability of either a positive or a negative change, relative
to the earliest plotted time for that marker, is at least \code{1 - threshold}.
Tiles that do not meet that criterion remain visible at reduced opacity.}

\item{smooth_trajectory}{Logical; add a secondary smooth of the posterior
median trajectory.}

\item{smooth_method}{Smoothing method, either \code{"loess"} or \code{"spline"}.}

\item{smooth_span}{Numeric span used by the loess smoother.}

\item{ci_levels}{Numeric credible interval levels.}

\item{ci_type}{Credible interval display: \code{"ribbon"}, \code{"line"}, or \code{"both"}.}

\item{observed_first}{Logical; shade the observed-history region for dynamic
prediction plots.}

\item{facet_by}{Faceting choice, either \code{"marker"} or \code{"none"}.}

\item{facet_scales}{Scale rule passed to marker facets.}

\item{combined}{Logical; combine requested panels when possible.}

\item{show_data}{Logical; display measured longitudinal responses.}

\item{show_observed_line}{Logical; connect measured responses within marker.}

\item{observed_style}{Named list controlling measured-response appearance.}

\item{prediction_style}{Named list controlling posterior trajectory appearance.}

\item{theme_fn}{Function returning a ggplot2 theme.}

\item{palette_marker}{Optional marker colour palette.}

\item{association_options}{Named list of options for
\code{type = "association"}. Supported entries are
\code{association_term}, \code{association_grid}, \code{association_range},
\code{association_points}, and \code{association_metric}. When
\code{association_grid} is not supplied, \code{plot.JoiNMeFit()} uses cached
model-implied raw support from the fitted association channels and only
falls back to knot support or observed-data heuristics when that cache is
unavailable. \code{association_range}, when supplied, overrides those default
range heuristics and constructs an evenly spaced raw-scale grid over the
requested interval.
\code{association_metric = "hazard"} plots the posterior contribution used by
the fitted model. For covariance-style channels (\code{corr}, \code{vcov}) this
contribution is zero-referenced at raw value \code{0} before multiplying by
\eqn{\alpha}; \code{association_metric = "transform"} plots the transform $f(x)$
alone and therefore omits the association-coefficient sign.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object, a combined plot, or a named list of plots.
}
\description{
Plot diagnostics, fitted trajectories, and association curves for JoiNMe fits
}
\details{
Fitted-data plotting from \code{JoiNMeFit} uses fitted posterior samples
and does not accept conditioning or future-prediction arguments
(\code{condition}, \code{conditioning}, \code{time_start}, \code{times}, \code{time_horizon}, or
\code{pred_control}). For population-level covariate profiles, call
\code{\link[=conditional_effects.JoiNMeFit]{conditional_effects.JoiNMeFit()}}. For subject-specific forecasts
conditional on observed marker history, call \code{\link[=predict]{predict()}} and then plot the
resulting \code{JoiNMeDynPred} object. At fitted trajectory times, covariates
other than time are held at their first observed value within each selected
subject-marker combination.
}
