% Generated by hand to match new API
\name{jm_family}
\alias{jm_family}
\title{Build per-marker family/link specifications}
\usage{
jm_family(name, link = NULL, inv_link = NULL)
}
\arguments{
\item{name}{Character family name (e.g. \code{"student_t"}, \code{"bernoulli"}).}
\item{link}{Optional link/inverse-link name. Allowed values: \code{"identity"}, \code{"log"}, \code{"logit"}, \code{"probit"}, \code{"exp"}.}
\item{inv_link}{Optional one-sided formula using \code{x}, e.g. \code{~ exp(x)}, \code{~ inv_logit(x)}, \code{~ probit(x)}.}
}
\value{An object of class \code{"joinme_family_spec"} for use in \code{families}.}
\description{Creates a per-marker family specification with explicit inverse-link mapping for longitudinal mean/location scale.}
\examples{
\dontrun{
families <- list(
  jm_family("student_t", link = "identity"),
  jm_family("bernoulli", link = "probit"),
  jm_family("poisson", inv_link = ~ exp(x))
)
}
}
