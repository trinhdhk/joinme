% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assoc.R
\name{assoc}
\alias{assoc}
\alias{posterior_assoc}
\alias{assoc.JoiNMeFit}
\title{Build posterior association effects for a fitted JoiNMe model}
\usage{
assoc(object, ...)

posterior_assoc(object, ...)

\method{assoc}{JoiNMeFit}(object, draws = NULL, seed = 1, digits = 3, summary = TRUE, ...)
}
\arguments{
\item{object}{A \code{JoiNMeFit} object.}

\item{...}{Unused.}

\item{draws}{Optional number of posterior draws to retain.}

\item{seed}{Random seed used when subsetting posterior draws.}

\item{digits}{Number of digits used when \code{summary = TRUE}.}

\item{summary}{Logical.
If \code{TRUE}, return posterior summaries.
If \code{FALSE}, return raw MCMC sample matrices.}
}
\value{
A named list with class \code{PosteriorAssoc}. Each list element contains
either a posterior summary table or a draws-by-term matrix for one
association channel.
}
\description{
Reconstructs the posterior association effects that enter the survival linear
predictor.

For weighted current-value and current-slope channels
(\code{cv_total}, \code{cs_total}, \code{cv_marker}, \code{cs_marker}),
the returned effect is the draw-wise product of the association coefficient and the marker weight,
divided by the number of markers to match the scale used in the fitted hazard contribution.

For scalar channels (\code{cv_mean}, \code{cs_mean}) the returned effect is simply the
posterior coefficient. For covariance-style channels (\code{corr}, \code{vcov}) the
returned effects are grouped by their labelled covariance component.
}
