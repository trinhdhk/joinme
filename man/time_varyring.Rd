% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{time_varyring}
\alias{time_varyring}
\title{Declare a time-varying simulation covariate generator}
\usage{
time_varyring(fun, n_step = NULL, steps = NULL, ...)
}
\arguments{
\item{fun}{Random generator function (for example \code{rnorm}, \code{rbinom}).}

\item{n_step}{Integer vector of length 1 or 2.
\itemize{
\item length 1: each subject receives exactly \code{n_step} step periods.
\item length 2: each subject draws its number of periods uniformly from
\code{min(n_step):max(n_step)}.
Ignored when \code{steps} is supplied.
}}

\item{steps}{Optional vector of positive integer time points where the
step changes occur exactly. Defaults to \code{NULL}.
Values greater than subject-specific stop times are ignored.}

\item{...}{Additional arguments forwarded to \code{fun}.}
}
\value{
Specification fed into \code{simulate_joinme()}.
}
\description{
Helper used inside \code{covariate_formulas} in \code{simulate_joinme()} to create
subject-specific stepwise covariates over time.
Elsewhere, this function does not evaluate anything.
Only a specification list is returned.
}
