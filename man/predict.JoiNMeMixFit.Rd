% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mix-methods.R
\name{predict.JoiNMeMixFit}
\alias{predict.JoiNMeMixFit}
\title{Predict from a latent-progress mixture}
\usage{
\method{predict}{JoiNMeMixFit}(
  object,
  newdataLong,
  newdataEvent = NULL,
  process = c("longitudinal", "event"),
  reuse_fitted_re = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted object of class \code{JoiNMeFit}.}

\item{newdataLong}{Data frame containing longitudinal histories for one or more subjects.
Must contain columns for id, time, marker, and response variables as specified in the original model formula.}

\item{newdataEvent}{Data frame containing event information and covariates used
in the survival model. It may contain one row per subject or interval-split
rows (\code{Surv(start, stop, status)} layout). Left- and interval-censored
survival encodings (\code{type = "left"}, \code{type = "interval2"}) are accepted.
When interval rows are supplied, dynamic prediction uses the latest row per
subject for event-side covariates. This argument may be omitted for a
longitudinal-only fit; neutral event rows are then constructed internally.}

\item{process}{Character vector specifying which predictions to compute.
Options: "longitudinal" (future trajectory), "event" (conditional survival probability). Default: both.}

\item{reuse_fitted_re}{Logical. If \code{TRUE}, every identifier in the supplied
data must have occurred during fitting. Prediction reuses that subject's
paired posterior random effects and covariance draws, without estimating a
second set of random effects. The default, \code{FALSE}, conditions newly sampled
effects on the supplied longitudinal history for dynamic prediction.}

\item{...}{Additional arguments passed to \link{predict}.}
}
\value{
A \code{JoiNMeMixDynPred} object inheriting from \code{JoiNMeDynPred}.
Conditional allocation draws are retained in
\code{draws$posterior_class} and may be summarised with
\code{\link[=posterior_class]{posterior_class()}}.
}
\description{
Runs the established dynamic prediction calculation while carrying the
fitted component probability, location and scale into the priors for newly
sampled latent effects.  The new subject's history therefore informs a
posterior probability vector over the fitted classes.  Combined compatible
blocks retain one shared allocation. With \code{reuse_fitted_re = TRUE}, the
fitted subject's realised effects and fitted allocation probabilities are
retained draw by draw instead of evaluating a new-subject allocation.
}
