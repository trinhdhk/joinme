% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{posterior_draws}
\alias{posterior_draws}
\alias{posterior_draws.JoiNMeFit}
\alias{posterior_draws.JoiNMeDynPred}
\alias{as.array.JoiNMeFit}
\alias{as.array.JoiNMeDynPred}
\title{Posterior draws for JoiNMe objects}
\usage{
posterior_draws(object, ...)

\method{posterior_draws}{JoiNMeFit}(
  object,
  variables = NULL,
  regex = FALSE,
  draws = NULL,
  seed = 1,
  what = c("all", "basehaz", "baseline_hazard"),
  format = c("draws_array", "draws_matrix", "draws_df"),
  ...
)

\method{posterior_draws}{JoiNMeDynPred}(
  object,
  variables = NULL,
  regex = FALSE,
  draws = NULL,
  seed = 1,
  format = c("draws_array", "draws_matrix", "draws_df"),
  ...
)

\method{as.array}{JoiNMeFit}(x, ...)

\method{as.array}{JoiNMeDynPred}(x, ...)
}
\arguments{
\item{object}{A supported JoiNMe object.}

\item{...}{Additional arguments forwarded from \code{as.array()} to \code{\link[=posterior_draws]{posterior_draws()}};
otherwise unused.}

\item{variables}{Optional character vector selecting variables after
relabelling.}

\item{regex}{Logical; if \code{TRUE}, interpret \code{variables} as regular
expressions.}

\item{draws}{Optional number of posterior draws to retain.}

\item{seed}{Integer seed used when subsetting draws.}

\item{what}{For \code{JoiNMeFit} objects, either \code{"all"} (default renamed
posterior variables), \code{"basehaz"}, or \code{"baseline_hazard"}.}

\item{format}{Output format. Supported values are \code{"draws_array"},
\code{"draws_matrix"}, and \code{"draws_df"}.}

\item{x}{A fitted \code{JoiNMeFit} or dynamic-prediction \code{JoiNMeDynPred} object
passed to the corresponding \code{as.array()} method.}
}
\value{
A posterior draw object in the requested format. The result is a
\code{posterior}-compatible object with renamed variables, suitable for
\code{posterior::summarise_draws()}, \code{posterior::subset_draws()}, and
\code{bayesplot}-style visualisation.
}
\description{
Returns posterior draws in \code{posterior}-compatible formats with user-facing
parameter names.

Compared with \code{\link[=extract]{extract()}}, \code{posterior_draws()} is the convenience layer for downstream
posterior workflows. It is designed for tasks such as \code{posterior}
summarisation, \code{bayesplot} visualisation, regex-based variable selection, and
any workflow that expects a standard draws object.

For fitted \code{JoiNMe} models, known Stan variables are relabelled from raw fit object
to friendly parameter names.
For dynamic prediction objects, stored draw blocks are flattened into a single
draws object with explicit id, scale, marker, and time labels.

Fitted-object draw arrays are cached inside the underlying R6 container after
the first request so later summaries, diagnostics, and plotting methods can
reuse the same renamed draw collection without re-reading the backend fit.

Use \code{posterior_draws()} when you want:
\itemize{
\item one posterior object containing renamed variables,
\item \code{posterior::subset_draws()} and regex-style variable filtering,
\item \code{bayesplot} directly, similar to \code{\link[=mcmc_plot]{mcmc_plot()}},
\item a standard draws array/matrix/data frame rather than a component-specific
extraction result.
\item \code{as.array} is a shorthand for \code{posterior_draws(format = "draws_array")}.
}

Use \code{\link[=extract]{extract()}} instead when you want:
\itemize{
\item one model component at a time (\code{"fixef"}, \code{"assoc"}, \code{"gamma_w"}, \code{"basehaz"}, etc.),
\item the explicit \code{term_map} telling you how user-facing labels map back to raw
Stan variables,
\item special structured results such as \code{what = "association_plot"},
\item prediction draw blocks separated by semantic role before flattening.
}
}
