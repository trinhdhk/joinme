% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-effects.R
\name{conditional_effects.JoiNMeFit}
\alias{conditional_effects.JoiNMeFit}
\title{Conditional effects for a fitted joint model}
\usage{
\method{conditional_effects}{JoiNMeFit}(
  x,
  effects = NULL,
  conditions = NULL,
  int_conditions = NULL,
  process = c("longitudinal", "event"),
  prob = 0.95,
  robust = TRUE,
  method = c("posterior_epred", "posterior_linpred"),
  longitudinal_estimand = c("population", "marker", "marginal_marker"),
  event_scale = c("hazard_ratio", "log_hazard_ratio"),
  resolution = 100L,
  surface = FALSE,
  markers = NULL,
  draws = NULL,
  seed = 1,
  plot = TRUE,
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{JoiNMeFit}.}

\item{effects}{Effects to evaluate. Supply a character vector such as
\code{c("time", "time:treatment")} to use the same requests wherever they are
valid, or a named list with \code{longitudinal} and \code{event} components to choose
process-specific effects. Interactions may contain at most two predictors.
If \code{NULL}, all model predictors and fitted two-way interactions are used.}

\item{conditions}{Optional data frame containing values of predictors on
which to condition. One set of effects is evaluated for every row.
\code{conditions$cond__}, when supplied, provides the facet label; otherwise row
names are used. Tables returned by \code{\link[=make_conditions]{make_conditions()}} can be supplied
directly.}

\item{int_conditions}{Optional named list controlling the evaluation values
of predictors named in \code{effects}. Each element may be a vector or a
function applied to the observed predictor. By default, the first numeric
predictor spans its observed range, factors use all levels, and a second
numeric predictor is evaluated at its mean and mean plus or minus one
standard deviation. This mirrors the principal \code{brms} convention.}

\item{process}{Character vector selecting \code{"longitudinal"}, \code{"event"}, or
both. The shorthand \code{"both"} and the default
\code{c("longitudinal", "event")} both return the two processes.}

\item{prob}{Probability covered by the equal-tailed posterior uncertainty
interval. The default is \code{0.95}.}

\item{robust}{Logical. If \code{TRUE}, posterior medians define \code{estimate__}; if
\code{FALSE}, posterior means are used.}

\item{method}{Longitudinal posterior scale. Supported values are
\code{"posterior_epred"} and \code{"posterior_linpred"}. The event process is
controlled separately by \code{event_scale}.}

\item{longitudinal_estimand}{Longitudinal quantity to evaluate.
\code{"population"} uses only the fixed-effect contribution, corresponding to
exclusion of group-level effects in \code{brms}. \code{"marker"} adds the fitted
marker-level deviation but excludes subject and marker-by-subject
deviations. \code{"marginal_marker"} first calculates each selected marker's
posterior prediction, including its marker-level deviation and fitted
inverse link, and then takes an equally weighted mean across markers within
every posterior draw. Thus, on the expected-response scale it estimates an
average of marker responses rather than the response obtained from an
averaged linear predictor.}

\item{event_scale}{Event-process estimand. \code{"hazard_ratio"} returns the
covariate-specific proportional-hazards multiplier \verb{exp(W gamma)};
\code{"log_hazard_ratio"} returns \verb{W gamma}.}

\item{resolution}{Number of support points for the first continuous
predictor. For a two-dimensional surface it is used on both axes.}

\item{surface}{Logical. If \code{TRUE}, two continuous predictors are evaluated
over a rectangular surface. If \code{FALSE}, the second continuous predictor is
represented by a small set of conditioning curves.}

\item{markers}{Optional character vector restricting longitudinal results to
selected marker levels. By default all fitted markers are used. For
\code{longitudinal_estimand = "marginal_marker"}, this argument defines the
markers entering the equally weighted posterior average.}

\item{draws}{Optional positive integer limiting the posterior draws used in
the calculation. \code{NULL} uses all available draws.}

\item{seed}{Integer seed used when posterior draws are subsampled.}

\item{plot}{Logical. If \code{TRUE}, construct and display conditional-effects
plots. If \code{FALSE}, return the underlying \code{JoiNMeConditionalEffects} data
object. The data object can subsequently be plotted with \code{plot()}.}

\item{...}{Additional arguments passed to the plotting method when
\code{plot = TRUE}, for example \code{points}, \code{rug}, \code{stype}, or \code{theme}; see
\code{\link[brms:conditional_effects]{brms::conditional_effects()}} for the corresponding graphical arguments.}
}
\value{
If \code{plot = FALSE}, a \code{JoiNMeConditionalEffects} object: a named list
with one component per requested process. Each process component is a
\code{brms_conditional_effects}-compatible named list containing one data frame
per effect. Longitudinal tables identify their estimand in
\code{longitudinal_estimand__}. If \code{plot = TRUE}, a similarly nested named list
of \code{ggplot} objects is returned invisibly after plotting.
}
\description{
\code{conditional_effects()} evaluates model-implied changes in the longitudinal
and event processes while holding all predictors not named in \code{effects} at
explicit conditioning values. Its interface follows
\code{\link[brms:conditional_effects]{brms::conditional_effects()}} where the joint-model structure permits a
direct correspondence.

Unlike a univariate regression, a \code{JoiNMeFit} contains two statistical
processes. The \code{process} argument therefore selects the longitudinal
expected response, the event relative hazard, or both. Longitudinal effects
may contain only the fixed-effect contribution, add the fitted marker-level
deviation, or average marker-specific predictions over the selected markers.
Subject- and marker-by-subject deviations are excluded from all three
longitudinal estimands. Event effects describe the proportional-hazards
multiplier \verb{exp(W gamma)} (or its logarithm), with the baseline hazard and
longitudinal association contribution held fixed. Consequently, event
contrasts isolate the part of the event process attributable to covariates
in \code{formulaEvent}.

The returned data follow the \code{brms} conditional-effects convention. Each
effect table contains \code{estimate__}, \code{se__}, \code{lower__}, \code{upper__}, and
\code{cond__}, plus \code{effect1__} and, for two-predictor effects, \code{effect2__}.
JoiNMe adds \code{process__}, \code{marker__}, \code{longitudinal_estimand__}, and
\code{event_type__} where applicable. This permits JoiNMe to delegate the
graphical construction to the tested \code{brms} plotting method rather than
maintaining a second plotting grammar.
}
\details{
Numeric predictors not named in an effect are held at their observed mean;
factors are held at their first level. Values supplied in \code{conditions}
override those defaults. Group-level subject deviations are excluded, as in
the default \code{re_formula = NA} behavior of \code{brms::conditional_effects()}.
Marker-level deviations are also excluded for the \code{"population"} estimand,
retained for the \code{"marker"} estimand, and integrated by finite averaging for
the \code{"marginal_marker"} estimand.

The event result is a relative-hazard effect rather than a dynamic survival
prediction. Dynamic survival probabilities depend on a subject's observed
marker history and conditioning time and therefore remain the estimand of
\code{\link[=predict.JoiNMeFit]{predict.JoiNMeFit()}}. Keeping these two estimands separate prevents a table
of baseline covariate effects from being mislabeled as subject-specific
dynamic prediction.
}
\examples{
\dontrun{
# Default longitudinal and event effects, returned as plot-ready data.
cond_eff <- conditional_effects(fit, plot = FALSE)

# Compare treatment profiles for a longitudinal time effect and an event
# age effect, using labels generated by brms::make_conditions().
profiles <- make_conditions(fit$dataEvent, vars = "treatment")
cond_eff <- conditional_effects(
  fit,
  effects = list(longitudinal = "time", event = "age"),
  conditions = profiles,
  process = c("longitudinal", "event"),
  plot = FALSE
)
plot(cond_eff, ask = FALSE)

# Request marker-specific longitudinal expected responses.
conditional_effects(
  fit,
  effects = "time:treatment",
  process = "longitudinal",
  longitudinal_estimand = "marker",
  int_conditions = list(treatment = c("control", "active"))
)

# Average marker-specific expected responses draw by draw.
conditional_effects(
  fit,
  effects = "time",
  process = "longitudinal",
  longitudinal_estimand = "marginal_marker",
  markers = c("marker_1", "marker_2"),
  plot = FALSE
)
}

}
\seealso{
\code{\link[brms:conditional_effects]{brms::conditional_effects()}}, \code{\link[=make_conditions]{make_conditions()}},
\code{\link[=predict.JoiNMeFit]{predict.JoiNMeFit()}}
}
