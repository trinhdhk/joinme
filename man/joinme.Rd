% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{joinme}
\alias{joinme}
\title{Fit joinme model via cmdstanr or rstan}
\usage{
joinme(
  formulaLong,
  dataLong,
  formulaEvent,
  dataEvent,
  formulaCorr = ~1,
  formulaDist = NULL,
  control = list(),
  draws = NULL,
  families = NULL,
  transforms = NULL,
  priors = joinme_priors(),
  fixed_marker_weights = FALSE,
  ...
)
}
\arguments{
\item{formulaLong}{Longitudinal formula defining fixed effects, id-level effects,
and the marker block. The marker block may optionally include an inner
\code{( ... | id )} term for marker-by-id random effects. When omitted,
marker-by-id effects are disabled (Q_idm = 0). Grouping terms may use
\verb{weighted(group, weights = <column>)} to declare
formula-scoped subject/group weights.}

\item{dataLong}{Long-format longitudinal data with columns for id, marker, time,
outcome, and covariates referenced in \code{formulaLong}.}

\item{formulaEvent}{Survival formula for baseline covariates and event model.}

\item{dataEvent}{One row per id event data with event time, event indicator, and
covariates referenced in \code{formulaEvent}.}

\item{formulaCorr}{Covariance regression formula for id-specific marker-by-id effects.
If the marker block omits the inner \code{( ... | id )}, marker-by-id effects are
absent and \code{corr} associations are not allowed.}

\item{formulaDist}{Optional list of formulas for distributional regression.
Supported LHS parameters are:
\itemize{
\item \code{sigma}
\item \code{nu}
\item \code{phi}
\item \code{alpha} (aliases: \code{alpha_skew}, \code{skew})
\item \code{phi_beta}
\item \code{tau_sde}
}

Three input styles are supported:
\enumerate{
\item Named list with RHS-only formulas, e.g. \code{list(sigma = ~ 1 + time)}.
\item Unnamed list with explicit LHS, e.g. \code{list(sigma ~ 1 + time)}.
\item Family-scoped LHS using brackets, e.g.
\code{list(sigma[family=student_t] ~ 1 + time, sigma[family=gaussian] ~ 1 + x1)}.
}

Semantics of scoping:
\itemize{
\item Without \verb{[family=...]}, the formula applies to all marker rows where that
parameter is defined.
\item With \verb{[family=...]}, the formula is applied only to rows of that family;
other rows receive zero contribution from that scoped block.
\item Multiple scoped formulas for the same parameter are allowed and are
estimated jointly with separate coefficients.
}

Random-effects terms with \code{|} are supported, but nested random-effects formulas
are not. Grouping factors in random-effects terms may use
\verb{weighted(group, weights = <column>)}.}

\item{control}{Named list containing sampling configuration.
\itemize{
\item cmdstanr::model$sample() arguments (e.g., chains, parallel_chains,
iter_warmup, iter_sampling, seed, refresh, adapt_delta, max_treedepth).
\item engine: "cmdstanr" or "rstan". Defaults to options(stan_preferred_engine).
\item threads_per_chain: integer; if > 1 uses \code{joinme_fit_threading.stan}.
For engine = "rstan", threading uses options(stan.thread = threads_per_chain).
\item grainsize: integer; reduce_sum grainsize for threading (default max(1, min(n_cores, ceiling(n_id/(4\emph{threads_per_chain}chains))))).
\item force_recompile: logical; recompile the Stan model if needed.
\item quadrature_nodes: optional positive integer total node target for survival
integration. Allowed values are exactly 7/15/31/41/51/61. Only the node
count is passed to Stan; GK nodes/weights are fixed in the Stan code.
\item corr_diag_link: "softplus" or "exp" for covariance regression diagonals.
\item tau_sde_fixed: optional fixed tau in (0,1) for skew-double-exponential.
}}

\item{draws}{Optional number of posterior draws used for summaries (not sampling).}

\item{families}{Marker-specific family specification (optional).
Can be a character vector of family names aligned to marker order, or a
list of \code{jm_family(...)} entries with per-marker links.
Supported links/inverse-links: \code{identity}, \code{log}, \code{logit}, \code{probit}, \code{exp}.}

\item{transforms}{Transformation specifications for association terms.
Prefer declaring them with \code{joinme_tf(...)}; raw named lists remain
supported. See Details for \code{ispline} and \code{ispline_penalised} semantics.}

\item{priors}{Prior declaration. Prefer \code{joinme_priors(...)}; raw named lists
with components \code{beta}, \code{alpha}, and \code{lkj} remain supported.}

\item{fixed_marker_weights}{Logical; if TRUE, marker weights are fixed at
provided \code{marker_weights} (or defaults). If FALSE, marker-weight perturbations
are estimated.}

\item{...}{Additional args passed to joinme_standata().}
}
\description{
Fits the joinme Stan model using cmdstanr (default) or rstan. The engine can be
set globally via options(stan_preferred_engine = "cmdstanr"|"rstan") or via
control$engine.
following the notebook, and supports association transformations and time-internal
scaling metadata.
}
\details{
Transformations are provided through a single \code{transforms} argument.
Each element (cv_total, cs_total, corr) is defined as a list specifying a type
and type-specific fields (see \code{build_standata_transforms()} for details).

Distributional regression can be specified in two equivalent forms:
\enumerate{
\item A named list with RHS-only formulas, e.g. \code{list(sigma = ~ 1 + time)}.
\item An unnamed list with explicit LHS parameter names, e.g. \code{list(sigma ~ 1 + time)}.
}

The time axis is internally scaled for numerical stability; reported coefficients
are rescaled within Stan so fixed effects remain interpretable on the original scale.

Marker weights (see \code{joinme_standata()}) are used to form marker-average summaries
for both current value (CV) and current slope (CS) association components. When
\code{fixed_marker_weights = FALSE}, signed perturbations are estimated around base
weights using \code{marker_weights + z_marker_weights} with
\code{z_marker_weights ~ N(0, 1)}. The effective marker intensities are used directly
from \code{w_raw} without additional normalisation and scale the association contributions.

Formula-scoped subject weighting is supported in random-effect grouping terms via
\verb{weighted(group, weights = <column>)}. For example:
\itemize{
\item \code{(1 + time | weighted(id, weights = id_w))}
\item \code{(0 + x1 + (1 + time | weighted(id, weights = id_w)) | weighted(marker, weights = marker_w))}
\item \code{sigma ~ 1 + (1 | weighted(id, weights = id_w))}
}

Weighted grouping affects latent random-effect priors for the referenced grouping
factor and applies subject-level likelihood weighting for the longitudinal and
survival contributions. Weight columns must be finite and strictly positive.

Association coefficients are denoted with the \code{alpha_} prefix to match joint-model
conventions and to avoid confusion with the linear predictor eta used throughout
the longitudinal and survival submodels. Total, marker, and mean associations use
a positive non-centred parameterisation: \code{alpha = z_alpha * sd_alpha}, which
stabilizes sampling while preserving the intended sign structure via marker weights.

The typical workflow is:
\enumerate{
\item Prepare \code{dataLong} and \code{dataEvent} with aligned ids and time scales.
\item Specify \code{formulaLong} and \code{formulaEvent} to define the longitudinal and survival submodels.
\item Choose \code{assoc} components and optional \code{transforms} for association terms.
\item Fit with \code{control} to manage sampling, threading, and reproducibility.
\item Validate convergence (R-hat, ESS, divergences), then predict.
}

Example:
\code{list(cv_total = list(type = "functional", expr = ~ log1p(x)), cs_total = list(type = "identity"), corr = list(type = "pwlin", x = c(-2, 0, 2), y = c(0.2, 1, 0.2)))}

Transformation parameterisation cheat sheet:
\itemize{
\item Omitted term, \code{NULL}, or \code{list(type = "identity")}:
identity transform (default).
\item \code{list(type = "functional", expr = ~ log1p(x))}:
functional transform; \code{expr} is required and has no additional defaults.
\item \code{list(type = "ispline", knots = c(-1, 0, 1), coeff = c(0, 0.3, 0.8, 1.1, 1.3), degree = 3)}:
monotone I-spline; \code{knots} and \code{coeff} are required, \code{degree} defaults to \code{3}.
\item \code{list(type = "ispline_penalised", x = seq(-2, 2, length.out = 50), y = exp(seq(-2, 2, length.out = 50)), n_knots = 6, degree = 3, lambda = 1)}:
penalised monotone I-spline in legacy plug-in mode; defaults are
\code{n_knots = 6}, \code{degree = 3}, \code{lambda = 1} when those values are omitted.
\item \code{list(type = "ispline_penalised", x = seq(-2, 2, length.out = 50), n_knots = 6, degree = 3, lambda = 1)}:
penalised monotone I-spline with Stan-estimated coefficients; if \code{knots}
are omitted they are derived from \code{x} quantiles using \code{n_knots = 6} by default.
\item \code{list(type = "pwlin", x = c(-2, -1, 0, 1, 2), y = c(0.2, 0.5, 1, 0.5, 0.2))}:
piecewise-linear transform; both \code{x} and \code{y} are required and there are no
additional defaults.
}

For monotone spline transforms:
\itemize{
\item \code{type = "ispline"}: provide \code{knots} and \code{coeff} directly (plus optional
\code{degree}); \code{x}/\code{y}/\code{lambda} are not used.
\item \code{type = "ispline_penalised"} (alias: \code{"ispline_penalized"}): provide
spline structure through \code{knots} (or \code{n_knots}) and smoothness penalty \code{lambda}.
\itemize{
\item If \code{y} is supplied, \code{joinme} first fits the monotone spline to training
pairs \verb{(x, y)} in R and passes fixed coefficients to Stan. These plug-in
coefficients use the same anchored convention as the Stan-estimated path:
first coefficient \code{0}, last coefficient \code{1}.
\item If \code{y} is omitted, Stan estimates the monotone spline coefficients
directly.
}
\item \code{x}: raw association-feature values used either to define training pairs or
to help derive knot locations.
\item \code{y}: optional target transformed values at those \code{x} points. Supplying \code{y}
activates the legacy plug-in fit; omitting it activates Stan estimation.
\item \code{lambda}: smoothness control (larger = smoother transform).
}

Additional arguments are forwarded to \code{joinme_standata()} (e.g., \code{assoc},
\code{basehaz}, \code{basehaz_degree}, \code{n_knots}, \code{time_var}, and \code{shrinkage}). Use
lme4-style \code{||} in \code{formulaLong} to request diagonal random-effect covariance.
}
\examples{
\dontrun{
formulaDist <- list(
  sigma[family=student_t] ~ 1 + time + (1 | id),
  sigma[family=gaussian] ~ 1 + x1,
  nu[family=student_t] ~ 1,
  alpha[family=skew_normal] ~ 1 + x1,
  phi[family=negbin2] ~ 1,
  phi_beta[family=beta] ~ 1,
  tau_sde[family=skew_double_exponential] ~ 1
)
}

}
