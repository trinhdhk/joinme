% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.JoinMeDynPred}
\alias{plot.JoinMeDynPred}
\title{Enhanced Plot Method for Dynamic Prediction from Joint Models}
\usage{
\method{plot}{JoinMeDynPred}(
  x,
  which = c("cumhaz", "longitudinal", "survival"),
  subject = NULL,
  trajectory_type = c("id_marker", "marker_pop", "overall_pop", "all_types"),
  scale = NULL,
  smooth_trajectory = TRUE,
  smooth_method = c("loess", "spline"),
  smooth_span = 0.3,
  ci_levels = c(0.5, 0.95),
  ci_type = c("ribbon", "line", "both"),
  observed_first = TRUE,
  facet_by = c("marker", "none"),
  facet_scales = "free_y",
  combined = TRUE,
  show_data = TRUE,
  show_observed_line = TRUE,
  observed_style = list(color = "black", shape = 21, size = 2, alpha = 0.6),
  prediction_style = list(color = "steelblue", fill = "steelblue", linewidth = 0.8, alpha
    = 0.2),
  theme_fn = ggplot2::theme_bw,
  palette_marker = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{JoinMeDynPred} returned by the \link{predict} method.}

\item{which}{Character vector indicating what to plot: "cumhaz" (default),
"longitudinal", or "survival". Multiple values are allowed.}

\item{subject}{Integer/Character vector. Which subject(s) to plot. If NULL,
plots all subjects in separate panels or returns a list.}

\item{trajectory_type}{Character. Which trajectory to show:
"id_marker" (default), "marker_pop" (marker-level average),
"overall_pop" (overall mean), or "all_types" for all three.}

\item{scale}{Optional longitudinal prediction scale to plot. Must match
available scales in the prediction object ("epred", "linpred", "predict").
If NULL, defaults to "epred" when available, otherwise the first
available scale.}

\item{smooth_trajectory}{Logical. Whether to smooth trajectories with splines
(default TRUE). Only applicable if sufficient observations.}

\item{smooth_method}{Character. Smoothing method: "loess" or "spline".}

\item{smooth_span}{Numeric in (0,1). Controls span/flexibility of LOESS smoothing
(default 0.3).}

\item{ci_levels}{Numeric vector. Credible interval levels to display
(default c(0.5, 0.95) for 50\% and 95\%).}

\item{ci_type}{Character. How to display uncertainty: "ribbon" (shaded area),
"line" (quantile lines), or "both".}

\item{observed_first}{Logical. Whether to visually separate observed history
(shaded background) from prediction region (default TRUE).}

\item{facet_by}{Character. "marker" to facet by marker type, "none" for single plot.}

\item{facet_scales}{Character. "fixed" or "free_y" for longitudinal faceting.}

\item{combined}{Logical. If TRUE and multiple plot types are requested, combine
them into a single layout (patchwork/cowplot).
\itemize{
\item For a single subject, returns one combined plot.
\item For multiple subjects, returns a named list of combined plots (one per subject).
\item If a combiner package is unavailable, falls back to the uncombined
subject/outcome structure for that subject.
}}

\item{show_data}{Logical. Whether to overlay observed data points (default TRUE).}

\item{show_observed_line}{Logical. Whether to connect observed points with lines
(default TRUE).}

\item{observed_style}{List with elements "color", "shape", "size", "alpha" for
appearance of observed data points.}

\item{prediction_style}{List with elements "color", "fill", "linewidth", "alpha"
for prediction line and ribbons.}

\item{theme_fn}{ggplot2 theme function (default \code{ggplot2::theme_minimal}).}

\item{palette_marker}{Character vector of colors for different markers, or
function like \code{ggplot2::scale_color_brewer()}.}

\item{...}{Additional arguments (unused).}
}
\value{
If a single subject and outcome requested: a \code{ggplot} object.
If multiple subjects and \code{combined = TRUE}: a named list where each element is
a per-subject combined plot (patchwork/cowplot) when backend support exists,
otherwise the corresponding uncombined per-subject list.
If multiple subjects and \code{combined = FALSE}: a named list grouped by subject,
each containing requested outcome plots.
If multiple outcomes: list with "longitudinal", "survival", and/or "cumhaz" elements,
or a single combined plot if \code{combined = TRUE}.
}
\description{
Produces publication-ready ggplot2 visualizations of dynamic predictions from
a joint model with flexible customization of longitudinal trajectories and
conditional survival curves.
}
\details{
\strong{Longitudinal Plots:}
\itemize{
\item The longitudinal scale is selected via \verb{.arg scale} (or inferred when NULL).
\item On \"predict\" scale: observed values are shown for times in interval
\verb{[0, time_start]} and predictions are drawn for \verb{[time_start, time_horizon]}.
\item On \"linpred\" or \"epred\" scale: predictions are shown for \verb{[0, time_horizon]},
with fitted values providing the pre-time_start segment when available.
\item Optional shaded region distinguishing observed from prediction periods.
\item Predicted trajectories with credible bands.
\item Multiple CI levels with decreasing alpha for visual hierarchy.
\item Optional smoothing for smoother appearance.
\item Support for multiple markers with automatic faceting.
}

\strong{Survival / Cumulative Hazard Plots:}
\itemize{
\item Conditional survival probability S(t | T_cond).
\item Conditional cumulative hazard H(t | T_cond) (default).
\item Credible bands with multiple levels.
}
}
