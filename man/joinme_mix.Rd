% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mix.R
\name{joinme_mix}
\alias{joinme_mix}
\title{Latent-progress mixtures for nested multivariate joint models}
\usage{
joinme_mix(
  formulaLong,
  dataLong,
  formulaEvent = NULL,
  dataEvent = NULL,
  formulaVCov = ~1,
  formulaDist = NULL,
  control = list(),
  draws = NULL,
  families = NULL,
  transforms = NULL,
  priors = joinme_priors(),
  basehaz = joinme_basehaz(),
  n_classes = 2L,
  formulaClass = ~1,
  class_type = "subject",
  class_dimensions = NULL,
  class_ordering = NULL,
  fit = TRUE,
  seed = NULL,
  ...
)
}
\arguments{
\item{formulaLong, dataLong, formulaVCov, formulaDist, control, draws, families, }{transforms,priors,basehaz,fit,
seed Arguments with the same meaning as in \code{\link[=joinme]{joinme()}}.}

\item{formulaEvent}{Optional survival formula.  Supply this together with
\code{dataEvent}, or leave both \code{NULL} for a longitudinal-only mixture.}

\item{dataEvent}{Optional event-process data.}

\item{n_classes}{Number of latent classes \eqn{G}; must be at least two.}

\item{formulaClass}{A one-sided formula for class-membership covariates, or a
named list with \code{subject} and \code{marker} formulae. A list containing
exactly \code{n_classes} formulae gives a separate predictor for every class.
Covariates must be constant within the corresponding allocation unit. The
intercept is represented by the baseline class probabilities.}

\item{class_type}{Character vector naming the random-effect class
types. The only accepted values are \code{"subject"}, \code{"marker"}, \code{"corr"},
and \code{"vcov"}.}

\item{class_dimensions}{Optional named list of integer coordinate indices,
one entry per selected class type. For a single type, an integer vector is
also accepted directly.}

\item{class_ordering}{Identification rule for the common class labels.
\code{"intercept"} orders only the first selected random-intercept location;
\code{"probability"} orders the baseline class probabilities; \code{"none"} imposes
no order. The default is \code{"intercept"} for a shared class formula and
\code{"none"} for a list of \code{n_classes} class-specific formulae.}

\item{...}{Additional arguments passed to \code{\link[=joinme_standata]{joinme_standata()}}, including
\code{assoc}, \code{id_var}, \code{marker_var}, \code{time_var}, and
\code{shrinkage}.}
}
\value{
If \code{fit = TRUE}, a \code{JoiNMeMixFit} object inheriting from
\code{JoiNMeFit}.  If \code{fit = FALSE}, a prepared \code{JoiNMeMixStanData} object.
}
\description{
\code{joinme_mix()} fits the JoiNMe model with a finite mixture on one or more
standardised random-effect blocks.  It has a separate entry point and
fitted-object class, whilst deliberately retaining the formula parser,
likelihood, association transforms, prediction programme, diagnostics, and
posterior utilities used by \code{\link[=joinme]{joinme()}}.

The allocation variables are analytically marginalised in Stan.  This is
important because Hamiltonian Monte Carlo cannot sample discrete class
labels.  Posterior class probabilities are reconstructed for every
subject or marker in generated quantities.
}
\details{
The longitudinal linear predictor remains

\deqn{
\eta_{id}(t)=x_{id}(t)^\top\beta+
z_i(t)^\top u_i+z_d(t)^\top v_d+
z_{id}(t)^\top w_{id}.
}

Latent-class modelling changes the distribution of selected \emph{standardised} random
effects, not the structural decomposition above.  For a selected vector
\eqn{a_j}, the ordinary density \eqn{D(a_j;0,1)} is replaced by

\deqn{
p(a_j)=\sum_{g=1}^{G}\pi_g
D\{a_j;\mu_g,\operatorname{diag}(s_g)\}.
}

\code{shrinkage = 0}, \code{1}, and \code{2} select Student-\eqn{t_6}, Laplace, and Normal
latent-class component densities, respectively. These choices do not set
the ordinary priors for \code{beta}, \code{alpha}, \code{iota}, marker effects or marker
weights; declare those independently with \code{\link[=jm_prior]{jm_prior()}}. The component locations and scales are
estimated. By default, Stan orders only the first selected random-intercept
location. Selected slopes and other coordinates remain unrestricted.
Alternatively, baseline class probabilities may be ordered, or ordering may
be disabled with \code{class_ordering}.

The supported class types are:
\itemize{
\item \code{"subject"}: the standardised subject effects underlying \eqn{u_i};
\item \code{"marker"}: the standardised marker effects underlying \eqn{v_d};
\item \code{"corr"}: the off-diagonal correlation coordinates in the
marker-by-subject covariance regression;
\item \code{"vcov"}: the lower-triangular variance--covariance coordinates in the
marker-by-subject covariance regression.
}

Compatible types share a single class allocation. In particular,
\code{c("subject", "vcov")} gives one \eqn{G}-class probability vector per subject.
The construction therefore has \eqn{G}
classes, rather than the Cartesian product of separate level-specific
classes. If both subject-indexed and marker-indexed types are selected,
they use the same \eqn{G} component labels and mixing proportions but retain
the natural allocation unit of their block.

By default the first two coordinates of each random-effect block define the
two-dimensional progress plane.  A one-dimensional block is retained as a
well-defined special case.  \code{class_dimensions} can select other
coordinates. \code{corr} and \code{vcov} are alternatives because both act on the
same covariance-regression block.

If \code{formulaEvent} and \code{dataEvent} are both \code{NULL}, a likelihood-neutral event
scaffold is created solely to reuse the common design code.  Event times and
event indicators are then set to zero in Stan data, so inference is based
only on the longitudinal process. Association terms are consequently
unavailable in this mode.
}
\examples{
\dontrun{
mixed_fit <- joinme_mix(
  y ~ time + (1 + time | id) +
    (1 + time + (1 + time | id) | marker),
  dataLong,
  survival::Surv(time, event) ~ treatment,
  dataEvent,
  n_classes = 3,
  class_type = c("subject", "vcov"),
  formulaClass = ~ treatment + age,
  priors = jm_prior(class = list(baseline_prob = rep(2, 3))),
  assoc = c("cv_total")
)

longitudinal_only_fit <- joinme_mix(
  y ~ time + (1 + time | id) +
    (1 + time + (1 + time | id) | marker),
  dataLong,
  n_classes = 2,
  class_type = "subject"
)
}
}
