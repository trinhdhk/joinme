% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-contrast.R
\name{plot.JoiNMeConditionalContrasts}
\alias{plot.JoiNMeConditionalContrasts}
\title{Plot JoiNMe conditional contrasts}
\usage{
\method{plot}{JoiNMeConditionalContrasts}(
  x,
  plot = TRUE,
  ask = FALSE,
  condition_variable = NULL,
  arrange = c("separate", "grid", "row", "column", "design"),
  ncol = NULL,
  nrow = NULL,
  design = NULL,
  widths = NULL,
  heights = NULL,
  guides = c("keep", "collect", "auto"),
  ...
)
}
\arguments{
\item{x}{A \code{JoiNMeConditionalContrasts} object.}

\item{plot}{Logical. If \code{TRUE}, draw each requested process.}

\item{ask}{Logical. Whether to prompt before drawing a subsequent process.}

\item{condition_variable}{Optional single column from \code{conditions} to place
on the horizontal axis. If \code{NULL}, fitted time is preferred when it varies,
followed by another varying numeric condition and then \code{cond__}.}

\item{arrange}{Arrangement of multiple process plots. \code{"separate"} retains
a named list and draws one figure at a time; \code{"grid"} uses an automatically
sized grid; \code{"row"} or \code{"column"} uses a single row or column; and
\code{"design"} follows the layout supplied through \code{design}.}

\item{ncol, nrow}{Optional positive whole numbers giving the grid dimensions
when \code{arrange = "grid"}.}

\item{design}{A patchwork design string or area description used when
\code{arrange = "design"}. Panels follow the process order in \code{x}.}

\item{widths, heights}{Optional positive numeric vectors giving relative
column widths and row heights in the arranged display.}

\item{guides}{How legends are treated across an arranged display. One of
\code{"keep"}, \code{"collect"}, or \code{"auto"}.}

\item{...}{Unused and reserved for graphical extensions.}
}
\value{
Invisibly, a named list containing one \code{ggplot} per process when
\code{arrange = "separate"}, or one \code{patchwork} display for every other
arrangement.
}
\description{
Displays the posterior centre and credible interval for each conditional
contrast. A numeric or date-valued condition may be used as a trajectory
axis; otherwise labelled conditions are shown as point intervals. Markers and
competing event types occupy separate panels.
}
